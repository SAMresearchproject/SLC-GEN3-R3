# CR120G Internal Split, Residue, Mirror-Odd X1 Complement

record_id: `CR120G_INTERNAL_SPLIT_RESIDUE_MIRROR_ODD_X1_COMPLEMENT`
result_class: `MATHEMATICAL_RESEARCH_BOUNDARY`
scientific_pass_claimed: `false`
disposition: `RESEARCH_BOUNDARY_CONSTRUCTED_SPLIT_RESIDUE_AND_STATEFUL_LIFT_DISCRIMINATED_CONDITIONAL_MIRROR_ODD_X1_COMPLEMENT_RANK80_MODEL_EXECUTES_INSTALLED_SAM_MAPPING_OPEN`

## Direct result

One exact rational constructed model passes every precommitted mathematical
gate and every frozen wrong control:

```text
parent - left - right - kappa       0
omit kappa                          e2; scalar residual 1
kappa_B(B0 identity)                e2
kappa_B(B1 swap e1/e2)              e1
stateful H1=[e1] readout            1
stateful H2=[e2] readout            0
static memoryless readout           same fixed value for both histories
Pi80 rank                           80
BALANCED / X1_ONLY / DEGENERATE     NULL
TRANSVERSE / MIXED                  NON_NULL; exact norm squared 5
```

The constructed conserving split closes only when its residue is retained. A
real mutation of the candidate action changes the residue, so the action and
its output are not interchangeable. The constructed recurrence carries state;
the frozen static control does not.

The candidate mirror decomposition reconstructs both faces exactly. Its odd
sector projected away from the predeclared `e0` X1 fixture direction produces
the frozen rank-80 signature and explicit null class. The bounded signed
permutation `Q` preserves X1, commutes with `Pi80`, and preserves exact norms and
null status. This is a bounded control, not general basis invariance.

The wrong projector `Pi_bad=diag(1,0,1,...,1)` fails to annihilate X1 and changes
the transverse squared norm from 5 to 4. Arbitrary coordinate removal is
therefore rejected by the frozen control.

## SAM authority boundary

This result is a coherent constructed candidate, not an installed SAM route or
a scientific PASS. The frozen language supplies typed counts and the legal
hierarchy, but no installed loop split, stateful operator, mirror parity
decomposition, vector-space representation of F81, X1 vector embedding, inner
product, or 80-coordinate decoder.

- `P80_PARTICLE_FACE_CONTENT` remains `STRUCTURAL_ONLY` and unpopulated.
- No particle row or identity is introduced.
- `H2_ARITY` remains an arity; no external Higgs identification is made.
- Candidate actions `B0` and `B1` are not identified with
  `B_CONTACT_OPERATOR`.
- Equal scalar values do not merge typed entities or constructed roles.
- No outside physical model or empirical evidence is used.

The CR120 frontier remains unchanged:

```text
PROPAGATE_CLOSURE       MISSING
LEDGER_SITE             MISSING
ADJACENT                MISSING
ADJACENT_LEDGER_STATE   MISSING
```

Installed/physical mapping remains open. The proposal has advanced only to a
validated internal mathematical research boundary.
