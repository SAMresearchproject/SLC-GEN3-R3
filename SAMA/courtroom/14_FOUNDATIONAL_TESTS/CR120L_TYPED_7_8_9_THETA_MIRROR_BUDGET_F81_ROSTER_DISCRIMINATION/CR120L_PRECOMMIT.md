# CR120L Typed 7-8-9 Theta Mirror Budget and F81 Roster Discrimination — Precommit

record_id: `CR120L_TYPED_7_8_9_THETA_MIRROR_BUDGET_F81_ROSTER_DISCRIMINATION`

task: `Run CR120L typed 7 8 9 theta mirror budget and F81 roster discrimination`

run_class: `CONSTRUCTIVE_NEW_WORK`

source_manifest_sha256: `fa49dd42d7c33c0fab386a1e994e2b2b699decbdbcbb2c468fcfa4f4f3c2c8a8`

## Purpose

Test whether the user-constructed 81-row workbook roster and CR120K's sealed
100-record independent inventory form a typed lower/full/upper budget mirror
under CR269's bow partition:

```text
Theta_out = 18
M         = 7 Theta = 126
R^2       = 8 Theta = 144
W9 weight = 9 Theta = 162

L_minus = 100 M              = 12600
L_mid   = 100 R^2            = 14400
L_plus  = 100 W9             = 16200
```

The run must distinguish an exact structural identity from authority to call
the workbook roster a canonical or physical particle inventory.

## Known-number disclosure

Every headline number was known before this precommit:

```text
workbook row count                 81
workbook M_native total         12600
CR120K independent record count   100
CR120K independent total        16200
CR269 M / R^2 / Theta       126 / 144 / 18
```

This is therefore not a blind numerical discovery test. It is a typed
composition, provenance, dependency, and selection-authority discrimination.
The arithmetic alone cannot produce a scientific PASS.

## Frozen sources

Only the exact files and hashes in `CR120L_SOURCE_MANIFEST.json` may be read.
The manifest is frozen at SHA-256
`fa49dd42d7c33c0fab386a1e994e2b2b699decbdbcbb2c468fcfa4f4f3c2c8a8`.

The workbook is a user research overlay, not canonical QP093A authority:

```text
C:\Users\drwho\OneDrive\Desktop\Updated Particle Rows2.xlsx
sha256 3d84689bb11551b24a43af0f195efaf0b95191b5811c876f8ad36867e2784fe8
bytes  93700
```

The canonical QP093A generator, catalog, CR120K, CR269, CR214, and the
task-specific preflight remain read-only.

## Typed quantities

Same-valued quantities must not be merged.

| id | type | value | authority |
|---|---|---:|---|
| `THETA_OUT_18` | `CarrierReleaseQuantum` | 18 | CR269 |
| `BOW_RETAINED_M_126` | `ScratchRetainedBudgetUnit` | 126 | CR269 |
| `R2_SURFACE_144` | `MirrorAreaBudgetUnit` | 144 | CR269 |
| `W9_WEIGHT_162` | `ClosureWitnessBudgetUnit` | 162 | CR120K plus exact mirror composition |
| `F81_ROSTER_CARDINALITY` | `WorkbookRowCount` | 81 | user workbook |
| `P100_PACKET_MULTIPLICITY` | `IndependentPacketCount` | 100 | CR120K; reused only as a typed multiplicity candidate |
| `L_MINUS_12600` | `ConstructedRosterNativeBudget` | 12600 | user workbook |
| `L_MID_14400` | `DerivedMirrorMidpointBudget` | 14400 | exact derivation only |
| `L_PLUS_16200` | `IndependentInventoryNativeBudget` | 16200 | CR120K |

The run may prove relations among these entities. It may not replace one type
with another, identify 81 rows with 100 packets, or install a one-to-one map.

## Frozen workbook extraction

Read `M_native 12600 - 81 Rows` directly from the XLSX ZIP/XML package without
executing macros or generated code. The extraction must verify:

1. the sheet exists;
2. the data range contains 81 candidate ids;
3. candidate ids are unique;
4. `SUM(M_native) = 12600` exactly from the stored decimal cells;
5. the cached workbook checks show rows 81, total 12600, residual 0;
6. the check formula explicitly names target 12600, preserving target-aware
   status;
7. no workbook formula or macro is executed.

## Canonical-row comparison

All 81 candidate ids must exist in the frozen QP093A catalog. Exact numeric
comparison is made from decimal values rather than display strings.

Expected semantic overlay deltas are restricted to these fields on exactly
five rows:

```text
fields: bin, stability_status, promotion_status
ids:
  QP093A-0065
  QP093A-0067
  QP093A-0068
  QP093A-0070
  QP093A-0071
```

Those rows remain canonically `unstable_resonance_rows` with
`UNSTABLE_HEAVY_WRITE_CANDIDATE`. Workbook labels beginning
`STABLE_USER_RECLASSIFIED` are annotation-layer proposals only. No canonical
row, registry, or historical verdict may be changed.

Excel display/cached rounding in non-`M_native` decimals must remain visible
but is not a source mutation. The authoritative decimals remain in QP093A.

## Precommitted roster reconciliation

Use the canonical QP093A lane
`stable_matter_rows + antimatter_conjugate_rows` as the comparison set:

```text
canonical lane                         105 rows, 16250.625
workbook/canonical intersection         76 rows,  2556
canonical ids omitted by workbook       29 rows, 13694.625
workbook-only versus canonical lane       5 rows, 10044
constructed workbook                    81 rows, 12600
```

The result must reproduce, not assume, this exact reconciliation.

## Precommitted conjugate-family test

Define the 24-row family before execution:

- matter plus/minus routes at `g=2`, `p in {1,2,3,4,6}`;
- matter plus/minus routes at `g=1`, `p=12`;
- their exact antimatter conjugates.

Required checks:

1. 12 matter rows and 12 antimatter rows;
2. exact route conjugation and no split pair;
3. equal `M_native` within each pair;
4. opposite charge sign within each pair;
5. matter surface adjustment zero;
6. antimatter surface adjustment nonzero;
7. observed candidates differ within every pair;
8. matter sum 6732;
9. antimatter sum 6732;
10. family sum 13464;
11. every family row is absent from the workbook roster;
12. `p=12,g=1` and `p=1,g=2` give exact 180/216 degeneracy.

This may establish a conjugate-balanced native removal block. It does not by
itself establish the authority of the complete 81-row selection.

## Extra selection packet

The run must expose the five additional canonical omissions:

```text
QP093A-0021    1.125
QP093A-0045   13.5
QP093A-0054   36
QP093A-0060   72
QP093A-0063  108
total        230.625
```

and the five user-overlay insertions relative to the canonical lane:

```text
QP093A-0065  1728
QP093A-0067  1620
QP093A-0068  1944
QP093A-0070  2160
QP093A-0071  2592
total       10044
```

Unless an already-frozen source contains an operator selecting these exact
packets without reference to the totals, their selection authority remains
`OPEN_NOT_INSTALLED`.

## Mirror gates

The exact mirror passes only if all are true:

```text
126 = 7 * 18
144 = 8 * 18
162 = 9 * 18
126 + 18 = 144
144 + 18 = 162

12600 = 100 * 126
14400 = 100 * 144
16200 = 100 * 162
(12600 + 16200) / 2 = 14400
(16200 - 12600) / 2 = 1800 = 100 * 18
```

`L_mid=14400` is a derived midpoint, not a separately observed inventory.

## Dependency interventions

Without mutating sources, recompute the mirror under symbolic perturbations:

- `Theta -> Theta + 1` while keeping typed coefficients 7, 8, 9;
- packet multiplicity `100 -> 99` and `100 -> 101`;
- roster cardinality `81 -> 80` and `81 -> 82` without changing the workbook.

The first two perturbations must change the associated budgets. Cardinality
perturbation must fail the workbook witness but must not silently change packet
multiplicity. This distinguishes topology/cardinality from budget content.

## Wrong controls

All must be detected and rejected:

1. merge `F81_ROSTER_CARDINALITY` with `P100_PACKET_MULTIPLICITY`;
2. call the workbook a canonical QP093A mutation;
3. install the five manual stable reclassifications as Courtroom authority;
4. claim one workbook row maps one-to-one to one packet;
5. claim removing only the 24-row family from the canonical lane gives 12600;
6. hide the five extra omissions or five overlay insertions;
7. treat 126, 144, and 162 as the same entity;
8. substitute Theta 17 or 19 while retaining the exact mirror;
9. treat 14400 as an observed roster rather than a derived midpoint;
10. label the target-aware workbook result blind;
11. use the QP093A-0299 Higgs reveal to authorize roster membership;
12. promote the 81-row roster to physical particle inventory;
13. mutate the workbook, QP093A, CR120K, CR269, CR214, or any registry;
14. hide rounding residuals or source/overlay semantic deltas;
15. import external physics or execute generated Python from the workbook.

## Required outputs

- `CR120L_ROSTER_81.csv`
- `CR120L_TYPED_QUANTITIES.json`
- `CR120L_MIRROR_BUDGET.json`
- `CR120L_ROSTER_RECONCILIATION.json`
- `CR120L_CONJUGATE_FAMILY_24.csv`
- `CR120L_SELECTION_AUTHORITY_AUDIT.json`
- `CR120L_DEPENDENCY_INTERVENTIONS.json`
- `CR120L_WRONG_CONTROLS.json`
- `CR120L_VALIDATION_REPORT.json`
- `CR120L_summary.json`
- `CR120L_result.md`
- `COMMAND_LOG.txt`
- candidate manifest, candidate hash, and `HASHES.txt`

## Expected disposition

If every exact and provenance gate passes, the expected disposition is:

`PASS_STRUCTURAL_7_8_9_THETA_MIRROR_100_PACKET_BUDGETS_WITH_F81_CONSTRUCTED_ROSTER_SELECTION_AUTHORITY_OPEN`

with:

```text
mathematical_verdict      PASS
result_class              STRUCTURAL_RESEARCH_BOUNDARY
scientific_pass_claimed   false
physical_mapping          OPEN_NOT_INSTALLED
roster_authority          USER_RESEARCH_OVERLAY_ONLY
```

Any failure of source hashes, workbook row/total checks, canonical identity,
conjugate pairing, CR120K/CR269 source assertions, mirror arithmetic, wrong
controls, or source immutability blocks the candidate.

## Hard boundaries

- Do not modify QP093A or the workbook.
- Do not promote manual stability labels.
- Do not install a new SAM operator, entity, registry entry, or physical law.
- Do not infer a bijection from 81 rows to 100 packets.
- Do not call exact arithmetic independent evidence when the target was known.
- Do not hide selection residuals, semantic deltas, or open authority.
- Do not answer around a failed gate.
