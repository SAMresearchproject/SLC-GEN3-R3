# CR120O All-Depth p9 Eight-Plus-One Native/qA Discrimination

record_id: `CR120O_ALL_DEPTH_P9_EIGHT_PLUS_ONE_NATIVE_QA_DISCRIMINATION`  
execution_status: `CLEAN`  
mathematical_verdict: `PASS`  
result_class: `STRUCTURAL_RESEARCH_BOUNDARY`  
scientific_pass_claimed: `false`  
disposition: `PASS_STRUCTURAL_TYPED_P9_8_PLUS_1_NATIVE_QA_LAW__PROPOSAL_PROMOTION_BLOCKED_NO_INDEPENDENT_DISCRIMINATOR`

## Direct result

The attached proposal's actual 8+1 test passes on every lawfully type-matched
QP093A single-write triple:

```text
typed p1/p8/p9 triples                 10
g=0 block                               5/5 PASS
g=1 untouched block                     5/5 PASS
exact Delta_native = 0                 10/10 PASS
generator-complete Delta_qA law        10/10 PASS
```

Thus, within the frozen QP093A single-write construction, p9 adds no new linear
`M_native` direction wherever p1, p8, and p9 are type-preserving matches.

## What the qA test found

The nonlinear residual is exact, but the short proposal formula is not the
whole conjugate law:

```text
Delta_qA = a * [c*12^g/9 + sigma*35/24]
```

The bare `a*12^g/9` cross-term passes the six zero-contact matter/neutral
triples and fails all four conjugate triples. The frozen surface debit/credit
operation supplies the additional signed `35/24` term. Once that already
installed operation order is retained, all ten qA residuals reproduce exactly.

This is a sharper result than the 16200 sum: it distinguishes native linear
redundancy from the nonlinear surface/qA record without deleting a p9 identity.

## All-depth challenge

The three g=2 matter groups also pass the scalar native and qA identities, but
they are not lawful typed triples under the proposal's own rule: p1 is stable
while p8 and p9 are unstable. They remain
`TYPED_INELIGIBLE_STABILITY_MISMATCH`.

All 51 composite p9 occurrences are retained as unmatched route-changing
challenges. Replacing a composite coordinate 9 by coordinates 8 and 1 changes
route arity and identity, so no same-route triple was fabricated. The hidden
p9 support row is nonlinear and non-matter; the carrier scalar 9 is not a p
coordinate.

## Sum-only neatness null

All 21 complete five-row family exclusions were enumerated. Every exclusion
leaves 100 rows, so the row count does not discriminate. Using sums only:

```text
integer remaining sums                 2/21
integer sums divisible by 18           1/21
hits to 12600, 14400, or 16200          1/21
exactly the p9,g0 remaining sum         1/21
```

The p9,g0 exclusion leaves exactly 16200, but that target was already observed.
These are descriptive multiplicities, not a blind significance claim. No mean
was computed or used.

## Model verdict

- `ALL_ELIGIBLE_DEPTH_8_PLUS_1`: structurally supported for the 10 typed
  stable/conjugate single-write triples, with the g2 type boundary explicit.
- `BASE_DEPTH_ONLY_8_PLUS_1`: unsupported; the untouched g1 block passes the
  same law and no independent g0-only rule is present.
- `PRIMITIVE_NINE`: preserved as the frozen QP093A control and not falsified by
  an identity that follows from its own generator.

The proposal therefore does **not** qualify for semantic or physical promotion.
No independent SAM-internal comparator distinguishes primitive-nine ontology
from a provenance-preserving reduced basis. No binding-energy, W9, L162,
particle-inventory, or registry claim is made.

The 12600, derived 14400, and 16200 values were held as current-sheet sums only.
The 81-row count was never used as a normalization, and neither workbook nor
any QP093A source was changed.
