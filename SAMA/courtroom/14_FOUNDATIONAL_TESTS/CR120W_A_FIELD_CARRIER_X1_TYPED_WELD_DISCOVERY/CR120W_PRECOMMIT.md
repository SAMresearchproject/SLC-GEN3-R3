# CR120W A-field-carrier / X1 typed-weld discovery precommit

Campaign: `CR120W_A_FIELD_CARRIER_X1_TYPED_WELD_DISCOVERY`  
Classification: `CONSTRUCTIVE_TYPED_MODEL_DISCRIMINATION`  
Same-run repair: `PROHIBITED`

## Question

CR282 proved that QP093A-0305 is the retired historical A-field row trace but
left the A-to-axis weld open. Shared scalar value one and descriptor similarity
were not sufficient.

CR120W adds a new and independent observation: Starbreaker carrier and matter
occurrences both reverse on the same local line at approximately 99 percent,
while the previously typed carrier dominates both relation-distance legs. This
campaign asks whether the shared-axis signature discriminates among all frozen
QP scalar-one candidates.

## Candidate universe

Read all 321 rows. Admit every row whose partition signature equals one. Do not
whitelist QP093A-0305 and do not use candidate ID as a feature. The frozen
expected census is 26 scalar-one occurrences. The legal nonlocal infrastructure
gate is expected to retain three nodes, but their identities remain unopened to
the scoring logic.

## Equal-treatment gates

### G1: legal nonlocal infrastructure

Require:

```text
arity = 0
disposition = NONLOCAL_NODE
structural_bucket in {CARRIER_INFRASTRUCTURE, HIDDEN_SUPPORT}
matter_row_allowed = no
not REJECTED_CONTROL
```

### G2: X1 axis shell

Require:

```text
partition_signature = 1
q_sign = neutral
q_abs = 0
native_charge_axis = carrier_axis
closure_status = CLOSED_CARRIER_SUPPORT
color_or_owner_closure = not_matter_owner
surface_depth = no_surface_depth
```

### G3: non-additive address role

Require the frozen CR222 classification to exclude the occurrence from the
twelve additive construction modes. Its native mass must be zero, and restoring
the partition-one proxy must be the 82/163 wrong control rather than an active
construction input.

### G4: shared field-axis mechanism

Require the frozen mechanism pair:

```text
route_combination = a_kernel_support
spin_or_hand_class = environmental_A_support
```

This is compared without weights against the two frozen alternatives:

```text
photon_road_carrier + transverse_vector
hidden_source_support[p=1] + unresolved_support/source_axis
```

The observed Starbreaker signature is shared across carrier and matter. It is
therefore compatible with an environmental interaction axis, not uniquely with
a transverse carrier or a charged source-support lane.

## Pass condition

Exactly one scalar-one occurrence must pass G1 through G4. All source custody,
census reconstruction, grammar preservation, ledger arithmetic, and wrong
controls must pass. The result may name a unique typed weld candidate.

It may not make the candidate an alias, identify the entities literally,
install `A_OPERATOR -> X1_AXIS_SELF_CHANNEL`, relabel a Starbreaker atom, or
rewrite CR282. The direct operator edge remains the next execution seam.

## Required wrong controls

1. Scalar-one-only equality.
2. A-field equals road light because both have partition one and zero mass.
3. A-field equals hidden p1 support because both have partition one.
4. Matter p1 rows treated as infrastructure.
5. Rejected fake closures admitted.
6. A-field restored as an additive 81 mode.
7. A-field row equated with the non-row A operator.
8. A-field carrier equated literally with X1.
9. B equated with X1.
10. Starbreaker carrier kind renamed A-field without a source join.

Stop after one complete frozen candidate enumeration and model comparison.
