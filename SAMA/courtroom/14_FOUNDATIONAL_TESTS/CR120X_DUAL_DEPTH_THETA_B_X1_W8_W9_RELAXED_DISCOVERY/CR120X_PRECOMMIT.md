# CR120X dual-depth Θ / B / X1 / W8-to-W9 relaxed discovery precommit

## Objective

Use the newest frozen Courtroom and Starbreaker records to test a constructive
mechanical reading, without requiring the hypothesis to establish physical
causality in its first integrative run.

The candidate chain is:

```text
A route depth d=1 supplies scale R^(d+1) = R² = N = 144
B contact partitions N -> (M, Θ_out) = (126, 18)
normalize by Θ=18: M/Θ=7, N/Θ=8=S8, L/Θ=9=W9
the released unit Θ_out/Θ=1 has the scalar size of X1's axis fee
B/contact acts through typed X1 to resolve S8 -> W9
```

`W8` is accepted as discovery shorthand for the existing
`S8_BINARY_SURFACE`; this run does not register a new W8 entity.

Θ is the already sealed `THETA18_PRIMARY_CARRIER`, not an invented contact
amplitude and not a fitted coefficient.

## Frozen distinction

`d_row` and `d_route` are different typed quantities. The photon and historical
A-field rows both carry QP `closure_depth=3`, while the non-row A operator
consumes the addressed route depth `d` in `R^(d+1)`.

The run will not identify:

- the historical A-field row with the non-row A operator;
- Θ with X1;
- B with X1;
- a moving Starbreaker carrier with X1;
- same scalar value with same entity.

## Current-data checks

1. Reconstruct the complete six-row real QP carrier roster from the 321-row
   map. The rejected fake carrier is a wrong control, not a candidate.
2. Reproduce CR256's 16 `d=0` and 16 `d=1` A rows and its 32/32 exact status.
3. Confirm `R^(d+1)=N` at `d=1` and compare `d=0,2,3` identically.
4. Reproduce the B partition `144=126+18`, retained/released ratios `7/8`
   and `1/8`, and `144=8*18`.
5. Divide the sealed M/N/L ladder by each current carrier candidate, using
   both its native carrier payload and its partition scalar. Record all ties;
   do not require a priori Θ uniqueness.
6. Test the exact normalized ladder `7,8,9` and the typed contacts to S8, X1,
   and W9.
7. Preserve the current CR120W A-field/X1 candidate result and the open direct
   A-operator edge.
8. Import the latest Starbreaker complete-history, matched-reopening, and
   axial-return summaries as independent directional contacts. Their lack of
   carrier exclusivity is not a failure gate.

## Discovery verdict rule

A discovery PASS requires exact source custody, current-row reconstruction,
the existing A depth-law result, exact B/Θ accounting, exact 7-8-9
normalization, fair all-carrier comparison, and typed/ledger preservation.

The strongest allowable PASS language is a coherent current-data bridge with
causal and runtime welds open. It is not a physical field detection, a new
particle, or an installed SAM operator.

No same-run repair. No source replacement after execution. No registry
mutation.
