# CR120Y Precommit — Θ18 Model-Wide Primary-Carrier Stress Test

## Frozen candidate

Before the model-wide scan or any scoring, this campaign freezes **Θ = 18, typed `PrimaryCarrier`**, as the candidate under test. This is the obvious current candidate because it is already named in the canonical hierarchy and appears at the intersection of the split, ledger, B-contact, QP, and Starbreaker records. The test does not grant it a pass.

## Purpose

Stress Θ18 fairly and seriously. The campaign asks whether the same typed occurrence survives six independent exact derivation routes, all current structural carrier-output substitutions, depth disruption, leave-one-evidence-family-out deletion, current QP row typing, and current Starbreaker ledger preservation.

## Frozen comparison class

The correct same-role structural comparison class comes from CR281:

`m3 = 6`, `D² = 9`, `Θ = 18`, `hV = 54`.

L162 is not in that class; it is terminal closure. QP carrier-row substitutions are retained as a separate current-data check and are not confused with the structural-output family.

## Frozen exact routes

1. `Θ = α_H D²`.
2. `Θ = R²/S`.
3. `Θ = N-M`.
4. `Θ = L-N`.
5. `Θ = (L-M)/2`.
6. `Θ = |A ∩ B|` for the two F81 ledger sides.

## Frozen stress tests

- Exact reconstruction of all six routes with rational arithmetic.
- Same-role substitution of 6, 9, 18, and 54 into the simultaneous B/ledger equations `N=S c`, `M=(S-1)c`, and `L=(S+1)c`.
- Leave each of six evidence families out once; no fold may depend on a candidate-ID whitelist or repair.
- Compare A route depths `d=0,1,2,3`; only the already registered `d=1` contact may equal `N=8Θ`.
- Reconstruct the current six-row QP carrier roster from operator classes, then test the two predeclared value modes used in CR120X.
- Read the current Starbreaker complete-history gates for 162-slot closure and 18/126/18 typing.
- Scan current result/summary artifacts only after candidate freeze. Corpus prevalence is descriptive and cannot prove the candidate.

## Pass rule

PASS requires every hard gate in `CR120Y_CONTRACT.json`. A failure is preserved; there is no same-run repair, redefinition, alternate candidate selection, or target-specific exclusion.

## Boundaries

PASS would support Θ18 as SAM's current **model-wide primary-carrier candidate** and distinguish it from the other structural carrier outputs for this role. It would not equate Θ with X1, turn Θ into matter, use 18 as a physical-mass coefficient, or by itself establish a physical ontology.

## Custody

The source manifest, contract, and this precommit are sealed before the runner is executed. All arithmetic, enumeration, corpus hashing, substitutions, folds, and artifact hashes are deterministic.
