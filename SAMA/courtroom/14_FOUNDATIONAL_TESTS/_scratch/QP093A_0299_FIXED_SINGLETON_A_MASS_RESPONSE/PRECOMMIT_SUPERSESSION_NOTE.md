# Scratch precommit supersession note

`SCRATCH_PRECOMMIT.md` was frozen before the runner existed. During the
permitted source-orientation pass, two directly controlling earlier lanes were
located:

- Courtroom CR103a/CR104 already lock an A-dependent Higgs-weight
  self-correction thesis while leaving the finite-A function open.
- STAM M020k already evaluates `m_H(A) = m_P * A^4 * alpha_em^5` with `A` as
  local substrate state.

No runner or result was produced from the first precommit. It remains preserved
unchanged as an audit record. `SCRATCH_PRECOMMIT_V2.md` is the controlling
precommit for this scratch and incorporates the newly located sources before
any runner is written.
