# CR201 Precommit

Question: Can the frozen G750c simulator artifacts support a Courtroom-grade
source-to-field bridge from particle/q_A source grammar to field inventory rows
without adding a parameter or importing Courtroom outcomes?

Allowed inputs are the G750c summary, q_A source ledger, particle route ledger,
and wrong-control table.

This test could have falsified: the claim that `q_A = m * (1 + r_bounce)` can
serve as a source ledger feeding the simulator bridge while preserving source
hashing, zero free parameters, and wrong-control rejection.
