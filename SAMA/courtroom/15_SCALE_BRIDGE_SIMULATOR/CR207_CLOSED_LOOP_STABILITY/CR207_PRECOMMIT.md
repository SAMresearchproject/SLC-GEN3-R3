# CR207 Precommit

Question: Can the simulator replay the source, field, Chladni, resolved-SW, and
wrong-control lanes as a closed loop without unexpected type violations?

Allowed inputs are the G756c summary and type-violation report.

This test could have falsified: the claim that the scale bridge can be replayed
as a closed loop with all run traces hashed and no unexpected type violations.
