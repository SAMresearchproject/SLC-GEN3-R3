# CR013 Collapse-Condition Candidate Sweep — Courtroom only

**Purpose.** CR013 DERIVER result sealed 2026-07-03 (sha `514f0721…`)
returned UNDERDETERMINED CLEAN with four Task-4 collapse conditions
the six-file whitelist does not close. This sweep asks, per
condition, what sealed structural content elsewhere in the Courtroom
repo already bears on it — with anti-retrofit discipline.

**Scope.** `c:\VS\The_Courtroom` only. The QP repo at
`C:\VS\quantum_phase` is deferred to a separate pass per Sean's
direction 2026-07-03.

**Anti-retrofit discipline.** We are NOT looking for content that
lands d_ref at 3.877 μm, 46.52 μm, 558 μm, or any other specific
value. We are looking for sealed structural rules that would close a
collapse condition regardless of what d_ref value results. Any
"candidate" whose only relevance is that it reproduces one of the
four DERIVER candidate values is a target-shape signal, not
closure content.

**Executive summary.**

| Condition | Status | Strongest candidate | Tier |
|---|---|---|---|
| 1 — Regime identification | **STRONG CANDIDATE, needs one bridge** | QP038 + CR103a 11/12 spaghettification threshold | DERIVED (QP038); PROVISIONAL (CR103a) |
| 2 — Projection level N | **PARTIAL, weak** | CR010 §3 S6 declaration + branch-19 CMB negative evidence | STIPULATED (CR010); indirect (CMB) |
| 3 — Mean-free-path form | **OPEN — no candidate found** | none | — |
| 4 — Absolute-d_ref observable | **OPEN — no candidate found** | none | — |

**Critical exclusion.** Sub-agent 2 initially cited CR012 v1.0's
"n_SLC_bench = 0" as evidence for Condition 2. This is the exact
content Sean identified as sabotage per
`CR012_NESTED_HOME_DISTANCE_AS_INFORMATION_FOUNDATION/
CR012_SABOTAGE_ACCOUNTING.md` §3.1 — the load-bearing structural
claim was defined in terms of the SLC bench context. That content
cannot be used as evidence for closing any collapse condition.
Excluded here.

---

## Condition 1 — Regime identification

**Question.** Which of {dilute self-reunion, dense cross-reunion,
ratio-only, other} governs the pop-bounce-intersect write-to-read
mechanism at a given operating condition? A closure needs the regime
to be derivable from substrate parameters, not stipulated.

### Candidate 1a — QP038 coherence-ratio boundary (DERIVED-with-citation)

**Location.** `12_QUANTUM_COMPUTING_AND_NETWORKING/_source_artifacts/
reports/QP038_PRIVATE_COMPOSITE_STABILITY_QUANTUM_SPAGHETTIFICATION_
BOUNDARY.md`

**What it seals.** Three-lane structural boundary:

- `single unresolved identity → local W/I fixed-point closure`
- `many-SW composite support → native binding and conserved cross-
  section support`
- `extended A-road propagation → coherence shear / quantum
  spaghettification`

Coherence-gate boundary points structurally:

- `below_1_of_12` → COHERENT_SHARED_CLOSURE (extended closure
  remains synchronized)
- `1_of_12` → COHERENCE_SHEAR_REORGANIZATION_BOUNDARY
- `at_or_above_12_of_12` → BREAKUP_RETARDED_CONTACT_FAILURE

Sealed formulas:

```text
coherence_ratio = β_rel · d_route · r_s / (r² − (L/2)²)
Δ_offset        = β_rel · |∫_front A_env ds − ∫_back A_env ds|
```

**Tier.** DERIVED-with-citation. Landmark 1/12 and 11/12 are base-12
substrate boundaries, not fitted.

### Candidate 1b — CR103a bounce-cost / A-dependence appeal (PROVISIONAL_DRAFT_PRE_SEAL_SIGN_OFF)

**Location.** `14_FOUNDATIONAL_TESTS/CR103a_BOUNCE_COST_AND_A_
DEPENDENCE_APPEAL/CR103a_result.md`

**What it seals** (verdict field `CR103a_BOUNCE_COST_A_DEPENDENCE_
STRUCTURAL_INSIGHT_LOCKED(PROVISIONAL_DRAFT_PRE_SEAL_SIGN_OFF)`):

- G435 PASS: `r_bounce = (A₀/2)·(q/2^D)`, mass-proportional bounce cost
- G470 PASS: SW-split bounce action theorem
- BB005 PASS: native ordering — 11/12 = MAXIMUM LOADING; 12/12 = A=1
  parent-road completion; A₀ = child-road baseline after reset
- User's Layer 3 (locked verbatim): "the bounce cost should be
  directly related to A- ... except at 11/12 when A can differ enough
  between the front and back of an object to destabilize
  intersections- quantum spaghettification."

Terminology discipline sealed:

```text
A₀ (constant) = 1/(12·π)          universal SW quantum
A  (field)    = local SW displacement density (position-dependent)
A₀ baseline     = lowest A in deep vacuum after parent-road reset
A_Earth_surface = A₀ + ~1.4×10⁻⁹  (Schwarzschild factor at Earth)
```

**Tier.** PROVISIONAL. Sign-off pending per its own verdict-field
tag. Structural upstream chain (G435/G470/BB005/QP038) is sealed.

### Assessment of Condition 1

**Strong candidate, one bridge needed.** QP038 + CR103a jointly seal
that regime identification is structural (not stipulated) and that
the boundary is at A = 11/12. At Earth surface, `A_Earth_surface ≈
A₀ + 1.4×10⁻⁹`, i.e., far below 11/12 by many orders of magnitude.
So on Earth-surface substrate the regime is deep in the low-A branch.

**Missing bridge.** The CR013 DERIVER's dilute-vs-dense split is
about **pop density in a loop-scale neighborhood** (endpoint self-
reunion vs cross-reunion). The QP038/CR103a 11/12 threshold is about
**front-back A-differential across an extended object** (coherence
shear). These two regime splits MAY be structurally the same
phenomenon — both are "when do multiple substrate events start
interfering with each other in ways that change the write-to-read
picture" — but the identification is not itself sealed.

**What would close Condition 1.** A structural CR that seals: "the
CR010 §1.5 cross-reunion regime split (dilute vs dense) is the same
regime split as the QP038/CR103a coherence-ratio boundary at 11/12."
Under such a bridge, Earth-surface operation sits deep in the
below-1/12 branch and Condition 1 collapses to the dilute /
COHERENT_SHARED_CLOSURE side.

**Target-shape check.** The candidates are sourced by A-field
threshold, not by any d_ref value. If Condition 1 closes to the
dilute side, the CR013 DERIVER's Candidate A (3.877 μm) would apply
— but that is a downstream consequence, not a driver of the sealing.
The 11/12 threshold is sealed from G435 + BB005 native ordering, not
from any bench-scale reference. Pass anti-retrofit discipline.

---

## Condition 2 — Projection level N (Home-nesting family)

**Question.** What is the R-power exponent N in CR010 §1.8's Home-
nesting dressing family for the write-to-read mechanism? Closure
options: (a) sealed derivation of N from a substrate parameter; (b)
sealed statement that SI-frame anchor per SAM_ON_EARTH_v1 §3 IS the
operational projection at N = 0.

### Candidate 2a — CR010 §3 S6 declaration (STIPULATED)

**Location.** `18_SAM_NATIVE_QC/CR010_SUBSTRATE_SPAGHETTIO_LATTICE_
FOUNDATION/CR010_PRECOMMIT.md` §3 S6.

**What it says (paraphrased from CR010 §1.8 / §3 S6).** R-power
dressings in downstream coupling laws are structural consequences of
Home-nesting projection levels, not device parameters.

**Assessment.** Declares R^N appears and IS structural (not
free-parameter), but does not pin N in any specific coupling
context. Tier: STIPULATED, not DERIVED. Useful only as ontological
scaffolding for a future N-pinning CR.

### Candidate 2b — Branch-19 CMB shape closure (indirect negative evidence)

**Location.** `19_BIG_BANG_SUBSTRATE_DERIVED_THERMAL_LADDER/`
(sub-agent 2 named CR035A2, CR036, CR036B, CR037A, CR037B, CR037C as
the closing chain — six PASS CRs; not independently re-verified in
this sweep).

**What it seals (per sub-agent).** Parameter-free CMB shape closure
against Planck PR3 and ACT DR4 at zero free parameters, using A₀ =
1/(12π) and Ω_m = R · A₀ = 12·A₀. No R^N dressing appears on any
length scale in the closure.

**Assessment.** Indirect. Cosmological CMB shape closes at zero free
parameters without needing R^N length dressing anywhere. This is
weak negative evidence that N = 0 is at least admissible at
cosmological scale. It is NOT positive evidence that N = 0 is forced
at the substrate-write coupling scale — different scale, different
observable class. Tier: indirect / partial.

**Target-shape check.** The CMB closure is a cosmological
consistency check, not a coupling-law observable. Its bearing on N is
purely absence-based reasoning ("if N > 0 were required, we would
see it here"). That absence argument is honest but weak.

### Candidate 2c — EXCLUDED (CR012 v1.0 "n_SLC_bench = 0")

**Excluded per CR012 sabotage accounting.** `CR012_SABOTAGE_
ACCOUNTING.md` §3.1 records that the sealed "n_SLC_bench = 0"
structural claim in CR012 v1.0 was scoped to the SLC bench context
in direct violation of the standing directive not to target SLC
criteria. Cannot cite for Condition 2 closure.

### Assessment of Condition 2

**Partial, weak.** No sealed candidate closes Condition 2 standalone.
CR010 §3 S6 declares the R^N family exists structurally; branch-19
CMB closure indirectly suggests N = 0 is at least admissible at
cosmological scale. Neither pins N in the write-to-read coupling
context.

**What would close Condition 2.** A structural CR that derives the
Home-nesting exponent N for the write-to-read mechanism from a
substrate parameter — OR a sealed statement (with structural
justification, not stipulation) that the SI-frame anchor per
SAM_ON_EARTH_v1 §3 IS the operational projection with N = 0. The
second is a cleaner path if defensible: SAM_ON_EARTH_v1 §3 already
argues the coupling law is ratio-form; a companion statement that
this identifies N = 0 would close conditions 2 and 4 together.

---

## Condition 3 — Mean-free-path form (if dense-limit)

**Question.** What is the functional form of the reach as a
function of pop density?

### No candidate found

Sub-agent 1's targeted search across 18_SAM_NATIVE_QC,
14_FOUNDATIONAL_TESTS, 12_QUANTUM_COMPUTING_AND_NETWORKING, and
12a_QC_QN_CARRIER_COMPRESSION_REFRESH found no sealed content
specifying:

- a pop cross-section
- an endpoint-propagation-dynamics formula
- a lab-scale pop density
- a specific reach(ρ) functional form

CR013 DERIVER Task 3 already recorded this openness. The Courtroom
sweep confirms: no candidate in Courtroom.

### Contingent bypass

If Condition 1 closes to the dilute side (via the QP038 bridge
above), Condition 3 becomes moot for the operating regime — the
reach in dilute limit is the loop-scale self-reunion length (CR013
DERIVER Candidate A tier chain), and no mean-free-path formula is
needed.

**Practical implication.** If the tractable path to close Condition 1
via the QP038 bridge succeeds, Condition 3 stays open but stops
mattering. If Condition 1 closes to the dense side, Condition 3
becomes the critical path.

---

## Condition 4 — Absolute-d_ref observable

**Question.** What sealed observable class fixes d_ref alone rather
than the ratio d_ref/d?

### No candidate found

Sub-agent 3 searched: CR005 (QNM), CR037B/CR037C (CMB shape closure),
LC11 (black-hole horizon replay), CR277 (element table), CR003b
(QNM higher modes), CR018/CR018b (BAO). All are either dimensionless
(QNM) or ratio-form (all others). No absolute-length observable
class is sealed anywhere in Courtroom.

**Explicit finding.** Neither Kerr-QNM ringdown, CMB acoustic
geometry, black-hole horizon algebra, nor nuclear mass-table closure
identifies an observable that fixes d_ref alone. All close at ratios.
This is consistent with SAM_ON_EARTH_v1 §3's ratio-form argument.

### Contingent bypass

If Condition 2 closes via the "N = 0 SI-anchor" reading (Candidate 2
option (b) above), Condition 4 collapses simultaneously — under that
reading, d_ref is not a separately-observable quantity in an SI
sense; the coupling law is ratio-form and that IS the operational
projection.

**Practical implication.** Condition 4 has no direct closure path
via existing sealed content. The only tractable closure path is
indirect through Condition 2's SI-anchor reading.

---

## Ranked next-step options

**Anti-retrofit reminder.** Any next step must be drafted with no
downstream d_ref value in scope. The CR012 v1.0 failure mode is
active — sealing a structural claim while targeting a specific
d_ref candidate is what CR013 exists to catch.

### Option A — Draft the QP038-to-CR010-§1.5 regime-bridge CR

**Scope.** Structural CR that identifies (or explicitly refuses to
identify) the CR010 §1.5 dilute-vs-dense regime split with the
QP038/CR103a 11/12 coherence boundary. Precommit written under
cleanroom discipline: forbidden strings include all four candidate
d_ref values (3.877 μm, 46.52 μm, 558 μm, and any specific bench
distance) plus SLC/patent/bench (per CR013 forbidden-string list).

**Expected outcome.** If bridge closes, Condition 1 collapses to the
Earth-surface-dilute side (A_Earth ≪ 11/12) and by Condition 3's
contingent bypass, no mean-free-path formula is needed. Two of four
conditions close from one structural CR.

**Risk.** The bridge might not close cleanly — pop density and A-field
gradient are related but not obviously identical. If the bridge is
FITTED rather than DERIVED, this CR would land as null-mapping.

**Tractability.** Medium. QP038's shear rule already relates to
A-field integrals; CR010 §1.5's cross-reunion condition relates to
endpoint density. The bridge needs a substrate-mechanics argument
that these are the same underlying phenomenon at different observer
scales.

### Option B — Draft the SI-anchor projection-level CR

**Scope.** Structural CR that examines whether SAM_ON_EARTH_v1 §3's
ratio-form SI-anchor argument implies N = 0 for the write-to-read
mechanism, OR whether it leaves N genuinely free. Precommit written
under cleanroom discipline with same forbidden-string list.

**Expected outcome.** If closes to N = 0 SI-anchor, Conditions 2 and
4 collapse simultaneously. If closes to "N genuinely free," Condition
2 stays open explicitly and Condition 4 stays open explicitly, but
now with sealed reason.

**Risk.** SAM_ON_EARTH_v1 §3 is a lock statement, not a derivation.
Extending it to an N-pinning claim requires substrate-scale argument
that connects SI-metrology-lock to write-to-read coupling-scale
projection. Non-trivial.

**Tractability.** Medium-low. The physics interpretation is subtle;
the CR would need careful precommit drafting.

### Option C — Stop and do the quantum_phase sweep first

**Scope.** Do the same sweep against `C:\VS\quantum_phase` before
drafting any new structural CR. QP repo may contain sealed content
that closes one or more conditions directly and would change the
tractability ranking above.

**Expected outcome.** Either a QP-side candidate closes a condition
(cleanest path to resolution), or the QP sweep returns similarly
open on all four conditions (in which case Options A/B become the
plan).

**Risk.** None. Purely diagnostic. Cost is the sweep itself.

**Tractability.** High. Fast diagnostic.

### Option D — Let the sealed state stand

**Scope.** No new structural CR. Accept UNDERDETERMINED as the
current honest verdict on d_ref, keep patent limbs struck, no
apparatus.

**Expected outcome.** Sealed state is stable. Any future closure
comes from unrelated substrate work that happens to bear on a
collapse condition. No forced timeline.

**Risk.** None. The audit trail is complete as-is.

**Tractability.** Trivial.

## Recommended next step (my read, not a directive)

**Option C first (quantum_phase sweep), then re-rank.** Cost is low,
diagnostic value is high, and Sean's note that QP has recent solid
work makes it plausible that a QP-side seal changes the picture. If
QP sweep returns open, Option A is the most tractable structural
draft — the QP038 bridge already has half the physics in place, and
closing Conditions 1+3 from one CR is efficient. Option B stays
available whether or not A executes.

## Provenance chain

```text
CR013_DERIVER_RESULT.md   sha256 514f07217fdcf7b1819c7a3bd9d5290060ddcd11405c19ee4776dcd9a3474816
CR013_VERIFIER_REPORT.md  sha256 9be2ec97e8d3165cbe1632e8aa4c19c009da697c65f4cdbc141d14590613a991
CR013_PRECOMMIT.md        sha256 17fa12eec1f1da430aebc7a011c0b3071622e47111d8666269937789f54965f3
CR012_SABOTAGE_ACCOUNTING sha256 13e81194dc4d91c00903af336568f63a47b26ff595d3e58458cf8994ff8d7a3e
STEWARDSHIP_DECLARATION   sha256 d9fe07c9b87bb52bc20b08bc1c8b24cb4fb312fbd35653b5d1030b14ed0a7e88
SAM_LC_WORK_ASSIGNMENT_v1 sha256 3a88142743f3d5487a55dc99e29f51edd097660e3cb27625c79aee3f5f29d4a4
SAM_ON_EARTH_v1           sha256 7b0f225821e2aea55e02b0868997fa2acd4ee88b4711ab1b1fb161366e1c5137
```

## Change log

```text
v1.0  2026-07-03  Sweep artifact written by SCRUBBER-lineage session
                  ("big-brother", session id 0ea2cc6e…) after three
                  Explore sub-agents returned per-condition findings.
                  Load-bearing agent claims spot-verified against
                  QP038, CR103a, and CR012 sabotage accounting.
                  CR012 v1.0 "n_SLC_bench = 0" explicitly excluded
                  as sabotage per Sean's own accounting. QP repo
                  sweep at C:\VS\quantum_phase deferred to a
                  separate pass.
```
