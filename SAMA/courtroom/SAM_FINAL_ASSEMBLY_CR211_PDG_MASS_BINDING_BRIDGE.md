# SAM Final Assembly Campaign
## CR211 Starbreaker–Particle Ledger Interface, PDG Physical Reveal, Native Scale Bridge, and Binding Reconciliation

**Recommended model:** GPT-5.6 Sol Extra High  
**Deterministic work:** Python scripts through `tools/run_sam_test.py`  
**Working root:** `C:\VS\The_Courtroom`  
**Primary source branch:** `15_SCALE_BRIDGE_SIMULATOR`  
**Source archive SHA-256:** `a975ce452a75571d9e872c2fecab9f9b7020f18e5729f17374864614d8d36dcd`  
**Branch-hash ledger SHA-256:** `3fb19131550dad09d2734d23666e01229445a433fc557f23156a5fcc95e99bb1`  
**Task class:** final scientific integration and staged reveal  
**Same-run repair:** forbidden  
**New fitted parameters:** forbidden unless a later separately precommitted campaign explicitly permits them  
**Scientific target:** assemble existing frozen structures correctly; do not restart discovery from scratch

This campaign fills the missing integration seam.

The archive already contains:

1. a source-to-field and typed-readout scale bridge;
2. an externally contacted resolved-parent and electroweak topology chain;
3. a Starbreaker three-dimensional directional contact;
4. an exact source-native octahedral ledger address factorization;
5. a complete target-withheld QP093A template and native-signature freeze;
6. a target-blind categorical PDG codebook campaign;
7. a final-holdout two-state champion;
8. an independently started nuclear-binding campaign.

The empty or unfinished seam is:

```text
CR211_STARBREAKER_PARTICLE_LEDGER_INTERFACE
```

The job is not to invent another theory. The job is to join the existing typed layers without erasing their boundaries.

---

# 1. Current Authoritative State

## 1.1 Scale bridge

Preserve:

```text
CR201 PASS
    source ledger -> q_A field inventory
    q_A = m(1 + r_bounce)
    zero new parameters

CR202 PASS
    mass-ledger, q_A, force, and clock readouts remain typed

CR203 PASS
    Chladni route support remains a boundary-route bridge, not a particle selector

CR204 BOUNDARY
    visible resolved parent reconstructed
    hidden source budget remains separate

CR204a PASS
    external H -> ZZ* -> 4l parent/daughter/final-state topology contact
    CR204 remains BOUNDARY

CR205 PASS
    scalar 9/16 remains scalar-lane specific
    it is not a universal fermion or gauge multiplier

CR206 PASS
    Earth-local, galaxy-many-nonzero, clustered scaffold, and PBH accumulation modes remain typed

CR207 BOUNDARY
    closed-loop simulator stability is internal discipline

CR207a PASS
    external-facing chain replay
    CR207 remains BOUNDARY

CR208 PASS
    43/43 wrong-control zipper

CR208a PASS
    resolved-SW external promotion zipper
    full electroweak theorem remains open

CR209 PASS
    H, Z, W, gamma, and fake-parent topology lanes separated
    no full coupling, amplitude, or branching-ratio theorem
```

Do not rerun or relabel these records merely to create a cleaner headline.

## 1.2 Starbreaker

Preserve:

```text
CR210 PASS
    1,218 three-dimensional runs replayed
    58/58 regression tests passed
    directional remnant ladders preserved
    no physical compactness, black-hole, time-to-formation, or hydrodynamic promotion
```

The working Starbreaker interpretation for this campaign is:

```text
A changes the number or density of complete slot ledgers
inside a measurable cubic region.

A does not change the internal carrier grammar of one ledger.
```

The interface must not alter per-ledger QP093A or K18 rules as A changes.

## 1.3 Octahedral ledger adapter

Historical records:

```text
CR210a
    execution failure before a structural verdict

CR210b
    FAIL, 15/17 checks

CR210c
    FAIL, 15/17 checks
```

Current successor:

```text
CR210d PASS
```

Exact CR210d findings:

```text
18 carrier + 126 matter + 18 shadow = 162

9 blocks x 18 slots = 162
6 fibers x 27 slots = 162

each 18-slot block:
    12 route addresses
    6 point addresses

each 27-slot fiber:
    3 carrier
    21 matter
    3 shadow

each side:
    9 carrier + 63 matter + 9 shadow = 81

H27 rank census:
    1 cell
    6 points
    12 edges
    8 faces

octahedral face-poset graph:
    27 nodes
    56 links
    48 signed-axis automorphisms

boundary quotient:
    81 + 81 - 18 = 144
    symmetric difference = 126
    occurrence sum = 162
```

Current CR210d verdict:

```text
CR210d_PASS_EXACT_SOURCE_NATIVE_W9_BY_K18_ADDRESS_FACTORIZATION
__SLOT_PARTITION_ROUTE_EDGE_OVERLAP_ORIENTATION_AND_PHYSICAL_INCIDENCE_OPEN
```

Keep open:

```text
physical particle shape
Starbreaker dynamical adjacency
physical route-edge identity
carrier/shadow physical orientation
F81 roster identity
PDG identity
binding improvement
```

## 1.4 QP093A grammar and native signatures

Preserve:

```text
Phase 0:
    321-row target-withheld token ledger frozen

Phase 1:
    321 = 285 legal local templates
        + 15 nonlocal/global/infrastructure nodes
        + 21 rejected records

    42 explicit conjugation dyads
    36 pair-reversal classes
    120 triad-permutation classes
    8 leaf compatibility classes

Stage B:
    235 ACTIVE_LIFT
    61 ZERO_FEE
    25 ZERO_SUPPORT

    n = M_native
    s = S_debit_or_credit
    o = M_observed_candidate
    q = qA_source_support
    t = tensor_carrier_support
    r = retained_write_support

    o = n - s
    q = 8t
    r = 7t
    q = t + r

    active typed lift:
        144(q-o) = q_abs * o
```

The one-body, pair, and triad rows are templates. They are not automatically 285 independent physical particles.

## 1.5 PDG categorical reveal

Preserve the complete history.

### Stage C1S

```text
557 named discovery targets
204 intact discovery families
physical masses, widths, lifetimes, decays, branching, and binding unopened

191,520 surjective eight-label -> six-flavor maps tested
12 exact content-tied codebooks remained

one exact best opposite-pair matching:
    1-5 | 2-4 | 3-6
```

### Stage C2A

```text
ABORTED
runner failed in an auxiliary sparse-support diagnostic
no recorded winner
validation door must not be reopened
```

### Stage C2B

```text
final holdout opened once
two-state championship only

CB2250 defeated CB2144

winner:
    CB2250__OPP_1-5|2-4|3-6

verdict:
    DIRECTIONAL_FINAL_CHAMPION

physical identity remains open
```

Important scope:

> CB2250 is the final champion inside the frozen two-state class.  
> It has not defeated the other ten C1S content-tied codebooks.

Do not call CB2250 the globally unique codebook until an orthogonal all-12 test is completed.

---

# 2. The Assembly That Is Now Available

The most natural typed join is:

```text
QP unary template
    -> content/point type

QP ordered pair template
    -> typed two-vertex relation

QP unordered triad template
    -> typed three-vertex relation

QP carrier row
    -> non-payload infrastructure

QP hidden support row
    -> lift-bearing connector infrastructure

QP global scalar parent
    -> one cell/global reveal role

QP rejected row
    -> forbidden-construction control
```

CR210d supplies the target geometry:

```text
1 cell
6 points
12 edges
8 triangular faces
```

The PDG content codebook supplies the eight-to-six fold needed to map the eight QP partition labels onto six flavor/content points.

This is the missing join.

Do not assign every pair to an octahedral edge or every triad to a triangular face automatically.

After the six-point content mapping is frozen, classify relations as:

## Pair relations

```text
SAME_POINT
    both labels decode to the same content point

ADJACENT_POINT_PAIR
    two different non-opposite points

OPPOSITE_POINT_PAIR
    one of the frozen opposite pairs

UNRESOLVED
    source typing or decoding is incomplete
```

## Triad relations

```text
OCTAHEDRAL_FACE_COMPATIBLE
    three distinct points
    one point chosen from each opposite pair

DEGENERATE_REPEATED_POINT
    one or more content points repeated

CONTAINS_OPPOSITE_PAIR
    triad includes both members of an opposite pair

MIXED_OR_UNRESOLVED
    relation is source-legal but not a simple octahedral face
```

This relation taxonomy must be derived before opening physical masses or binding values.

---

# 3. Hard Causal Order

The final sequence is:

```text
complete and seal the already-started binding baseline
    ->
freeze source lineage and current authority
    ->
build CR211 interface with no physical-value reveal
    ->
open categorical physical identities and crosswalk
    ->
freeze all mass-readout candidates
    ->
open PDG masses through a new three-door value vault
    ->
run all-12 orthogonal mass championship
    ->
join the frozen binding residual packet
    ->
test whether revealed motifs explain residual structure
    ->
export to SAM UI and Starbreaker
```

Do not use a downstream result to repair an upstream stage.

---

# 4. STOP 0 — Binding Baseline Seal

Before this campaign opens any new PDG physical-value field, verify that the current first binding campaign is sealed.

Required binding-baseline receipt:

```text
input isotope roster
training/test/extended split
all coefficients
operator strengths
free-parameter count
Theta treatment
W9 treatment
Higgs-parent treatment
per-isotope residuals
family residuals
wrong controls
runner hash
input hashes
result hash
```

Also record the baseline’s knowledge state:

```text
PDG codebook not used
physical QP-to-PDG identities not used
CR211 interface not used
new shape classifications not used
post-reveal repair not used
```

If the campaign is not yet sealed, stop with:

```text
WAITING_FOR_PRE_PDG_BINDING_BASELINE_SEAL
```

Do not open physical masses.

---

# 5. Stage A — Final Source and Authority Freeze

Create:

```text
FINAL_ASSEMBLY_SOURCE_MANIFEST.json
FINAL_ASSEMBLY_AUTHORITY_GRAPH.json
FINAL_ASSEMBLY_LINEAGE_AUDIT.csv
FINAL_ASSEMBLY_CURRENT_STATUS.md
FINAL_ASSEMBLY_PRECOMMIT.md
```

## Required authority graph

Represent exact claim-level authority.

Examples:

```text
CR210c structural check:
    historical FAIL

CR210d exact W9 x K18 address factorization:
    PASS

CR210d physical incidence:
    OPEN

Stage C2A validation winner:
    NONE / ABORTED

Stage C2B two-state categorical champion:
    CB2250

CB2250 global all-12 codebook status:
    OPEN

PDG physical mass status:
    UNOPENED

binding application:
    BASELINE_ONLY until Stage E
```

No historical record may be rewritten.

---

# 6. Stage B — Build CR211

Create and execute:

```text
CR211_STARBREAKER_PARTICLE_LEDGER_INTERFACE
```

This is a structural interface campaign.

Do not open:

```text
new PDG identities
PDG masses
widths
lifetimes
decays
branching ratios
binding observations
Starbreaker outcome optimization
```

## 6.1 CR211 input objects

### Starbreaker region

```json
{
  "event_id": "opaque",
  "spatial_cell_id": "opaque",
  "A_local": "typed_dimensionless_value",
  "measurable_volume": "typed_volume",
  "slot_ledger_count": "integer",
  "ledger_density": "typed_density",
  "qp_grammar_hash": "sha256",
  "cr210d_adapter_hash": "sha256"
}
```

### Complete ledger instance

```json
{
  "ledger_instance_id": "opaque",
  "slot_count": 162,
  "carrier_slots": 18,
  "matter_slots": 126,
  "shadow_slots": 18,
  "w9_blocks": 9,
  "k18_slots_per_block": 18,
  "h27_addresses": 27
}
```

### QP template instance

```json
{
  "template_id": "opaque",
  "source_row_id": "opaque",
  "template_class": "UNARY|PAIR|TRIAD|CARRIER|SUPPORT|SCALAR|REJECTED",
  "partition_labels": [],
  "charge_orientation": null,
  "closure_depth": null,
  "native_signature_id": "opaque",
  "payload_status": "PAYLOAD|INFRASTRUCTURE|GLOBAL|REJECTED"
}
```

## 6.2 Interface fields

Generate:

```text
ledger_instance_id
starbreaker_cell_id
slot_index
w9_block
k18_channel
dimension
side
route
h27_rank
h27_address
qp_template_id
qp_source_row_id
qp_template_class
qp_partition_labels
decoded_content_points [NULL before Stage C]
relation_geometry [NULL before Stage C]
native_signature_id
payload_status
binding_role [UNASSIGNED]
authority
source_hashes
```

## 6.3 CR211 structural gates

Require:

1. Every interface row traces to one CR210d address and one source QP template or explicit null.
2. No QP template is promoted from type to physical occurrence merely because a slot exists.
3. One-body, pair, triad, infrastructure, scalar, and rejected classes remain distinct.
4. The scalar parent occurs globally once per declared scope, not once per constituent.
5. Carrier and hidden-support rows cannot occupy ordinary matter payload slots without a separate operator.
6. Same-scalar entities remain separate.
7. A changes ledger multiplicity, not per-ledger grammar.
8. All 162-slot, 9x18, 6x27, 81/144/126/162 checks reproduce.
9. No mass, binding, target identity, or outcome field influences placement.
10. Wrong controls fail.

## 6.4 CR211 wrong controls

Test:

```text
random slot assignment
candidate-ID whitelist
workbook 81 roster as geometry truth
all scalar nines merged
all pairs called edges
all triads called faces
carrier inserted as matter payload
hidden support inserted as particle
scalar parent repeated per ledger constituent
A used to mutate the QP grammar
Starbreaker outcome used to choose placement
binding residual used to choose placement
```

## 6.5 CR211 verdict ceiling

Maximum Stage B verdict:

```text
PASS_CR211_TYPED_STARBREAKER_QP_LEDGER_INTERFACE
__PHYSICAL_CONTENT_MASS_BINDING_AND_DYNAMICAL_INCIDENCE_OPEN
```

Stop and seal CR211 before Stage C.

---

# 7. Stage C — Categorical Physical Identity Reveal

## 7.1 Freeze the tested champion

Record exactly:

```text
tested_two_state_champion = CB2250
eliminated_two_state_candidate = CB2144
global_12_codebook_champion = OPEN
```

## 7.2 CB2250 mapping

The frozen codebook is:

```text
p=1  -> target vertex 1
p=2  -> target vertex 1
p=3  -> target vertex 6
p=4  -> target vertex 3
p=6  -> target vertex 5
p=8  -> target vertex 2
p=9  -> target vertex 4
p=12 -> target vertex 2
```

The official PDG MCID digit convention must be sourced and hashed before converting target vertices to quark names.

Expected convention to verify:

```text
1 = d
2 = u
3 = s
4 = c
5 = b
6 = t
```

If verified, the scoped CB2250 interpretation is:

```text
p1, p2   -> d
p8, p12  -> u
p4       -> s
p9       -> c
p6       -> b
p3       -> t
```

Do not merge `p1` with `p2` or `p8` with `p12`. They remain distinct QP lanes even when they decode to the same content vertex.

## 7.3 Opposite-pair geometry

Freeze:

```text
1-5 | 2-4 | 3-6
```

Under the verified PDG digit convention this becomes:

```text
d-b
u-c
s-t
```

This is a structural opposite-pair candidate.

It is not automatically a physical octahedron in space.

## 7.4 Apply categorical mapping

Apply the frozen decoder to all legal local templates.

Generate:

```text
CR211_QP_TO_PDG_CONTENT_CROSSWALK.csv
CR211_PAIR_RELATION_GEOMETRY.csv
CR211_TRIAD_RELATION_GEOMETRY.csv
CR211_CONTENT_COVERAGE.csv
CR211_CONTENT_AMBIGUITY.csv
```

For each QP template report:

```text
source partition labels
decoded flavor/content labels
target class
charge orientation
same/adjacent/opposite relation for pairs
face-compatible/degenerate/opposite-containing relation for triads
source stability
source closure
source promotion status
ambiguity
```

## 7.5 Reveal final target identities

Open the final target identity/crosswalk custody exactly once under a new precommit.

Do not open physical values yet.

Report:

```text
coverage
exact content-class matches
family matches
charge matches
unmatched targets
unmatched templates
ambiguous mappings
wrong controls
```

Do not claim a unique particle-to-row assignment when multiple templates share the same decoded content.

## 7.6 Categorical verdicts

Possible:

```text
PASS_CR211_TYPED_QP_TO_PDG_CONTENT_REVEAL
__CB2250_TESTED_CLASS_CHAMPION_GLOBAL_CODEBOOK_AND_MASS_OPEN

BOUNDARY_CATEGORICAL_CONTENT_CONTACT_PRESENT
__UNIQUE_TEMPLATE_IDENTITY_OPEN

FAIL_CB2250_CATEGORICAL_PHYSICAL_REVEAL
```

Stop and seal before physical masses.

---

# 8. Stage D — Orthogonal All-12 Physical-Mass Reveal

Physical mass was not used in the categorical codebook campaign. It is the next orthogonal channel.

This stage must test all 12 C1S content-tied codebooks, not only CB2144 and CB2250.

## 8.1 New mass-value custody

Using the same whole-family discovery/validation/final split, create a new encrypted physical-value vault.

Allowed target fields:

```text
central mass value
mass uncertainty
mass status or limit flag
declared units
```

Keep closed:

```text
width
lifetime
decay modes
branching ratios
binding data
```

## 8.2 Freeze mass readouts before opening values

Locate every pre-existing source-backed mass/readout contract.

At minimum audit:

```text
n = M_native
o = M_observed_candidate = n - s
q = qA_source_support
existing scale-bridge visible-parent readout
existing field/source bridge
existing unit conversion contracts
```

Do not count algebraically dependent readouts as independent candidates:

```text
t = q/8
r = 7q/8
```

Do not invent:

```text
one scale per particle
one scale per family
one scale per QP partition
one offset per class
nearest-number matching
post-reveal column switching
```

If no source-backed physical mass readout exists, preserve:

```text
BOUNDARY_CONTENT_REVEAL_PASS_PHYSICAL_MASS_OPERATOR_ABSENT
```

and stop.

## 8.3 Three-door mass campaign

### Discovery door

May select among the fully precommitted source-backed readouts.

Must score all 12 frozen codebooks.

### Validation door

Select one readout and one codebook under the frozen metric.

No repair.

### Final door

Apply the frozen winner once.

## 8.4 Mass metric

Use a scale-appropriate metric such as:

```text
absolute log mass residual
uncertainty-normalized residual where valid
coverage
one-to-one assignment penalty
unmatched target penalty
unmatched source penalty
family-level residual
```

The exact metric and weights must be frozen before discovery masses are opened.

## 8.5 Mass wrong controls

```text
shuffle masses within class
shuffle flavor labels
allow row reuse without penalty
choose best numeric column per target
fit one scale per target
fit one scale per family
admit carriers as matter payload
admit hidden support as particle
repeat scalar parent
ignore charge or content incompatibility
nearest-number-only matching
```

## 8.6 Mass verdicts

```text
PASS_ALL12_ORTHOGONAL_PDG_MASS_REVEAL
PASS_CATEGORICAL_CODEBOOK_MASS_SCALE_PARTIAL
BOUNDARY_CATEGORICAL_REVEAL_PASS_UNIVERSAL_MASS_READOUT_OPEN
FAIL_QP_PDG_MASS_BRIDGE
```

---

# 9. Stage E — Binding Reconciliation

Import the already sealed pre-PDG binding campaign.

Do not refit it in the same run.

## 9.1 Identify proton and neutron motif classes

Using the frozen categorical mapping, enumerate source triad templates decoding to:

```text
uud
udd
anti-u anti-u anti-d
anti-u anti-d anti-d
```

Preserve every source distinction:

```text
partition labels
depth
charge
surface status
stability status
native signature
relation geometry
```

Do not choose the proton or neutron template by observed nuclear binding.

## 9.2 Join to the frozen binding residual packet

For every isotope in the baseline, derive only predeclared explanatory features:

```text
proton motif count
neutron motif count
one-body point counts
pair SAME/ADJACENT/OPPOSITE counts
triad FACE_COMPATIBLE/DEGENERATE/OPPOSITE counts
hidden-support requirements
active-lift count
zero-fee count
surface-exposed count
mixed-interface count
neutron excess
existing shell/pair/operator flags
```

## 9.3 Post-reveal audit

Ask:

```text
Do large residuals cluster by newly revealed motif features?

Do light-nucleus failures correspond to missing or degenerate cell closure?

Does neutron excess increase opposite-pair, mixed-interface, or hidden-support demand?

Does typed exclusion of W9, carrier, support, or scalar-parent double counting
remove a systematic error?

Do existing CR274 family operators correspond to source motif predicates?
```

This stage may identify a candidate mechanism.

It may not repair the baseline.

## 9.4 Binding continuation rule

A new binding formula may be designed only in a later separately precommitted campaign.

Maximum Stage E verdict:

```text
PASS_PDG_REVEAL_EXPLAINS_FROZEN_BINDING_RESIDUAL_STRUCTURE
__NEW_BINDING_FORMULA_NOT_RUN

PASS_BINDING_ACCOUNTING_CLEANUP_NO_HELDOUT_IMPROVEMENT_CLAIM

BOUNDARY_PDG_MOTIF_JOIN_COMPLETE_BINDING_MECHANISM_OPEN

FAIL_PDG_MOTIF_BINDING_EXPLANATION
```

---

# 10. Stage F — Starbreaker and UI Handoff

After CR211, categorical reveal, mass stage, and binding audit are sealed, create one versioned interface contract.

```text
STARBREAKER_QP_PDG_SHAPE_INTERFACE_V1.json
```

It must state:

```text
A controls ledger multiplicity/density
per-ledger grammar remains fixed
CR210d supplies address geometry
QP supplies content/relation templates
PDG reveal supplies physical content labels
mass readout supplies physical scale if passed
binding remains a separate geometry/accounting readout
```

The UI may display and query the result.

The UI may not change it.

Required UI endpoints:

```text
GET /api/v1/final-assembly/status
GET /api/v1/ledgers/{id}
GET /api/v1/templates/{id}
GET /api/v1/content-map
GET /api/v1/geometry-map
GET /api/v1/mass-result
GET /api/v1/binding-audit
GET /api/v1/provenance/{id}
```

---

# 11. Required Artifacts

Create:

```text
FINAL_ASSEMBLY_SOURCE_MANIFEST.json
FINAL_ASSEMBLY_AUTHORITY_GRAPH.json
FINAL_ASSEMBLY_LINEAGE_AUDIT.csv
FINAL_ASSEMBLY_CURRENT_STATUS.md
FINAL_ASSEMBLY_PRECOMMIT.md
FINAL_ASSEMBLY_HASHES.txt

CR211_STARBREAKER_PARTICLE_LEDGER_INTERFACE/
    CR211_PRECOMMIT.md
    CR211_DECLARED_PREMISES.json
    CR211_SOURCE_MANIFEST.json
    CR211_INTERFACE_SCHEMA.json
    CR211_LEDGER_INSTANCE_REGISTER.csv
    CR211_QP_TEMPLATE_REGISTER.csv
    CR211_ADDRESS_TEMPLATE_JOIN.csv
    CR211_WRONG_CONTROLS.csv
    CR211_runner.py
    CR211_result.md
    CR211_SUMMARY.json
    CR211_PROVENANCE.json
    HASHES.txt

PDG_REVEAL_STAGED_PHYSICAL_IDENTITY/
    IDENTITY_PRECOMMIT.md
    CB2250_MAPPING.json
    ALL12_AMBIGUITY_REGISTER.csv
    OFFICIAL_PDG_DIGIT_CONVENTION.json
    FINAL_IDENTITY_ACCESS_LOG.csv
    QP_TO_PDG_CONTENT_CROSSWALK.csv
    PAIR_RELATION_GEOMETRY.csv
    TRIAD_RELATION_GEOMETRY.csv
    CONTENT_COVERAGE.csv
    CONTENT_AMBIGUITY.csv
    IDENTITY_RESULT.md
    HASHES.txt

PDG_REVEAL_PHYSICAL_MASS/
    MASS_CUSTODY_CONTRACT.json
    MASS_DISCOVERY_VAULT.enc
    MASS_VALIDATION_VAULT.enc
    MASS_FINAL_VAULT.enc
    MASS_READOUT_CONTRACT.json
    ALL12_DISCOVERY_SCORES.csv
    VALIDATION_SCORES.csv
    FINAL_SCORES.csv
    MASS_WRONG_CONTROLS.csv
    MASS_RESULT.md
    HASHES.txt

PDG_BINDING_RECONCILIATION/
    BINDING_BASELINE_RECEIPT.json
    PROTON_NEUTRON_TEMPLATE_CENSUS.csv
    ISOTOPE_MOTIF_FEATURES.csv
    FROZEN_BINDING_RESIDUAL_JOIN.csv
    RESIDUAL_FEATURE_AUDIT.csv
    ACCOUNTING_DOUBLE_COUNT_AUDIT.csv
    BINDING_RECONCILIATION_RESULT.md
    HASHES.txt

STARBREAKER_QP_PDG_SHAPE_INTERFACE_V1.json
FINAL_ASSEMBLY_RESULT.md
FINAL_ASSEMBLY_SUMMARY.json
FINAL_ASSEMBLY_RELEASE_MANIFEST.json
```

---

# 12. Final Verdict Tree

Return one primary verdict.

## Full integration pass

```text
PASS_SAM_FINAL_ASSEMBLY
__CR211_LEDGER_INTERFACE
__TYPED_PDG_CONTENT
__ORTHOGONAL_MASS_BRIDGE
__BINDING_RECONCILIATION
```

Requirements:

- CR211 passes;
- categorical identity reveal passes;
- one all-12 codebook wins a physical-mass final holdout;
- mass readout is source-backed and not target-fitted;
- binding baseline remains immutable;
- post-reveal motif features explain a precommitted residual structure;
- no same-run repair;
- no new per-target parameters.

## Strong structural finish

```text
PASS_SAM_FINAL_TYPED_ASSEMBLY
__CR211_AND_PDG_CONTENT_COMPLETE
__UNIVERSAL_MASS_OR_BINDING_OPERATOR_OPEN
```

Use when the typed assembly and physical content reveal close but mass or binding remains open.

## Useful boundary

```text
BOUNDARY_FINAL_ASSEMBLY_STRUCTURE_COMPLETE
__PHYSICAL_SCALE_OR_BINDING_MECHANISM_OPEN
```

## Fail

```text
FAIL_SAM_FINAL_ASSEMBLY
```

Use only when the frozen layers cannot be joined without contradiction, target leakage, source mutation, or unsupported coercion.

## Invalid

```text
INVALID_SOURCE_LINEAGE
INVALID_BINDING_BASELINE_NOT_SEALED
INVALID_TARGET_CUSTODY
INVALID_SAME_RUN_REPAIR
```

---

# 13. Hard Stop Rules

1. Do not reopen Stage C2A validation.
2. Do not rewrite CR210b or CR210c.
3. Do not call CB2250 globally unique before all-12 orthogonal testing.
4. Do not open masses before categorical mapping and mass readouts are frozen.
5. Do not use physical masses to change the codebook.
6. Do not use binding residuals to choose proton/neutron templates.
7. Do not modify the sealed binding baseline.
8. Do not infer literal spatial octahedra solely from H27 combinatorics.
9. Do not mutate per-ledger QP grammar as A changes.
10. Do not start water, molecular, or desalination work in this campaign.
11. Stop after the final integration result and handoff.

---

# 14. Copy-Ready Master Prompt

```text
Execute SAM_FINAL_ASSEMBLY_CR211_PDG_MASS_BINDING_BRIDGE.md exactly as written.

Use GPT-5.6 Sol Extra High for scientific integration and deterministic Python
for all enumeration, joins, hashes, controls, and scoring.

This is the final assembly campaign.

Do not restart the theory from scratch.
Do not rerun or rewrite sealed predecessor results merely to simplify them.

Use the exact 15_SCALE_BRIDGE_SIMULATOR source tree and verify its branch hash
ledger.

Preserve:

    CR204 and CR207 as BOUNDARY
    CR204a, CR207a, CR208, CR208a, and CR209 as scoped PASS records
    CR210 as scoped Starbreaker directional-contact PASS
    CR210a as execution failure
    CR210b and CR210c as historical FAIL
    CR210d as the current 17/17 structural successor
    Stage C2A as aborted with no winner
    Stage C2B as CB2250 champion only inside the tested two-state class

Before opening new PDG physical-value fields, require the already-started
binding campaign to be sealed as the pre-PDG baseline.

Fill the currently empty integration seam:

    CR211_STARBREAKER_PARTICLE_LEDGER_INTERFACE

Build the typed interface:

    Starbreaker A / cubic region
        -> slot-ledger multiplicity

    CR210d
        -> 162-slot W9 x K18 and H27 address geometry

    QP093A
        -> unary, pair, triad, carrier, support, scalar, and rejected templates

    PDG codebook
        -> physical content labels

    mass bridge
        -> physical scale only after a source-backed readout is frozen

    binding baseline
        -> post-reveal residual reconciliation only

Use the structural join:

    unary template -> point/content type

    pair template -> SAME, ADJACENT, OPPOSITE, or UNRESOLVED two-point relation

    triad template -> FACE_COMPATIBLE, DEGENERATE, CONTAINS_OPPOSITE,
                      or MIXED/UNRESOLVED three-point relation

    carrier -> infrastructure

    hidden support -> lift-bearing infrastructure

    scalar parent -> global cell/reveal role

    rejected row -> forbidden-construction control

Do not call every pair an edge or every triad a face.

Freeze and execute CR211 before opening physical identities.

For the categorical reveal, preserve the Stage C2B tested-class winner:

    CB2250

with mapping:

    p1  -> 1
    p2  -> 1
    p3  -> 6
    p4  -> 3
    p6  -> 5
    p8  -> 2
    p9  -> 4
    p12 -> 2

Source and hash the official PDG quark-digit convention before converting
vertices to names.

Preserve duplicate QP lanes that map to the same content vertex.

Freeze the opposite-pair geometry:

    1-5 | 2-4 | 3-6

as structural geometry only.

Open final physical identities exactly once.
Do not open masses in the same stage.

After categorical identity is sealed, build a new three-door physical-mass
vault using the existing whole-family split.

Before opening masses, freeze every source-backed candidate mass readout.
Do not invent per-particle or per-family scales.
Do not choose among M_native, M_observed_candidate, qA, or another column after
seeing target masses.

Run all 12 C1S content-tied codebooks through the mass campaign.
CB2250 must not receive an unreported scoring bonus.

Then import the immutable pre-PDG binding baseline.

Enumerate source templates decoding to proton and neutron content.
Do not choose proton/neutron templates from observed nuclear binding.

Join frozen isotope residuals to predeclared motif features.
Do not repair the binding formula in the same run.

Generate the complete CR211, identity, mass, binding-reconciliation, provenance,
wrong-control, custody, and release artifacts.

Stop after one final integration result.

Return:

    source lineage status
    CR211 verdict
    categorical identity verdict
    tested-class and all-12 codebook status
    physical mass verdict
    binding reconciliation verdict
    remaining boundaries
    final release manifest hash
    one primary final verdict
```
