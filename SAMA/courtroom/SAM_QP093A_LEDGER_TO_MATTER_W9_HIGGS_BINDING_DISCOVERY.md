# SAM QP093A Ledger-to-Matter Discovery Campaign
## 16,200 / 100 Rows, 12,600 / 81 Rows, the W9 Closure Bridge, QP093A-0066, the Higgs Row, and Binding

**Recommended model:** GPT-5.6 Sol High  
**Fallback:** GPT-5.5 Extra High  
**Do not use Sol Ultra unless a repo-wide source conflict cannot be resolved.**  
**Working root:** `C:\VS\The_Courtroom`  
**Task class:** constructive scientific discovery followed by frozen validation  
**Prospective v0.3 holdout eligible:** false  
**Scientific reruns:** allowed only where explicitly defined below  
**Language status:** SAM Language may be queried for typed names and current authority, but it is not independent scientific evidence

This campaign is not a request to reject the finding because a finished operator is not already registered.

The job is:

1. reproduce the exact 16,200-over-100 and 12,600-over-81 observations;
2. determine what structural accounting they may represent;
3. test the row-level relationship between the typed \(8+1\rightarrow9\) closure hierarchy and the QP093A rows;
4. investigate QP093A-0066 and QP093A-0299 directly;
5. determine whether any resulting rule can improve nuclear binding without target leakage, per-isotope patching, or turning 126/162 into fitted coefficients.

The campaign has a **discovery phase** and a **validation phase**.

Discovery may inspect the observed rosters and totals. Validation must freeze any candidate rule before testing it outside the discovery lane.

---

# 1. Controlling Inputs

Use and hash at minimum:

```text
Updated Particle Rows.xlsx
Updated Particle Rows2.xlsx

CR120R_QP093A_P9G0_WITNESS_PACKET_EXCLUSION_AND_100_ROW_L162_NATIVE_LEDGER.zip

CR119 typed closure hierarchy and language handoff contract
CR267 tensor 9 closure witness
CR269 B/contact operator
CR282 appeal validation
CR233 / CR229 ledger and carrier-role records
CR253 80-row promoter
CR254 charged row law
CR255 neutral row law
CR256 A-operator and hard-zero result
CR257 compact d=1 forms
CR274 binding model and gated operators
CR277 126-row binding extension
CR249 and CR250 preserved binding dead ends
```

Search for any earlier QP093A analysis that discusses:

```text
p=1, p=8, p=9
g=0, g=1, g=2
M_native
qA_source_support
tensor_carrier_support
retained_write_support
closed_scalar_loop
Higgs reveal
126, 144, 162
```

Record exact source paths, hashes, sealed status, and whether each source is authoritative, exploratory, superseded, or only a reading aid.

Do not treat chat transcripts or workbook annotations as sealed authority. They may supply discovery leads.

---

# 2. Preserve Existing Results

Do not overwrite or weaken CR120R or CR120S.

Preserve:

```text
CR120R:
    exact five-role packet algebra
    p=9,g=0 exclusion overlay
    100 rows
    exact M_native total = 16,200
    exact mean = 162
    result class = structural research boundary

CR120S:
    F81 / M126 numeric weld known
    source-backed row-selection operator not yet installed
    projection rule remains open
```

This campaign is allowed to inspect the 81-row workbook because it is explicitly a **discovery campaign**.

It may produce candidate selectors.

It may not call a candidate selector an active physical operator until that selector survives the validation phase.

---

# 3. Canonical Notation Audit

The user’s working shorthand may appear as:

```text
W8 -> X1 -> W9
S8 -> X1 -> W9
W9 -> X1 -> W9
```

Do not silently choose among these.

Locate the controlling sealed relation and report the canonical typed form.

Expected source candidate:

```text
RESOLVE(
    S8_BINARY_SURFACE,
    B_CONTACT_OPERATOR,
    X1_AXIS_SELF_CHANNEL
) -> W9_CLOSURE_WITNESS
```

Then distinguish that typed relation from the QP093A arithmetic shadow:

```text
[p=8,g,role] + [p=1,g,role] = [p=9,g,role]
```

The campaign must keep separate:

```text
S8_BINARY_SURFACE
C8 carrier occurrence
P8 support occurrence
QP093A p=8 coordinate

X1_AXIS_SELF_CHANNEL
C1 carrier occurrence
P1 support occurrence
QP093A p=1 coordinate

W9_CLOSURE_WITNESS
C9 carrier occurrence
P9 support occurrence
QP093A p=9 coordinate
```

Equal scalars are not equal entities.

---

# 4. Phase I — Workbook and Source Audit

## 4.1 Workbook inventory

For both workbooks, record:

```text
sheet names
sheet dimensions
column names
row counts
formulas versus stored values
duplicate candidate IDs
hidden rows or columns
manual annotations
user-reclassification columns
changed cells between the two workbooks
```

Create a cell-level diff for:

```text
Updated Particle Rows.xlsx
versus
Updated Particle Rows2.xlsx
```

At minimum audit:

```text
QP093A-0066
QP093A-0299
all rows in the 81-row sheet
all rows whose M_native or promotion status changed
```

## 4.2 Required source discrepancy check

The supplied row for QP093A-0299 has:

```text
M_native = 126000
```

The current second workbook may contain a different value in the main sheet.

Do not choose one silently.

Report:

```text
value in workbook 1
value in workbook 2
formula or literal status
file hash
cell address
modification provenance if available
which source is controlling
```

If the change was made only to force the 81-row total, classify it as target-aware and do not use it as scientific evidence.

If it is an intentional typed exclusion, identify the source rule that authorized it.

Continue the campaign with both interpretations if the provenance cannot be resolved.

---

# 5. Phase II — Reproduce the Two Ledger Observations

## 5.1 The 100-row result

Reproduce exactly:

```text
starting candidate lane = 105 rows
excluded packet = five p=9,g=0 occurrences
remaining rows = 100
remaining M_native total = 16,200
remaining mean M_native = 162
```

Report:

\[
16200 = 100\cdot162 = 100L.
\]

Reproduce the complete five-role packet law from source.

Determine whether the uniqueness claim is:

```text
arithmetic uniqueness given the target mean
typed uniqueness supplied independently by W9
both
neither
```

Do not count an algebraic consequence twice as independent evidence.

## 5.2 The 81-row result

Open the explicit sheet:

```text
M_native 12600 - 81 Rows
```

Reproduce exactly:

```text
selected rows = 81
M_native total = 12,600
```

Report:

\[
12600 = 100\cdot126 = 100M.
\]

Also report the apparent accounting ladder:

\[
100L=16200,
\]

\[
100N=14400,
\]

\[
100M=12600,
\]

\[
L=N+\Theta,
\qquad
M=N-\Theta,
\]

\[
\frac{100L+100M}{2}=100N,
\]

\[
\frac{100L-100M}{2}=100\Theta.
\]

This is exact numeric compatibility.

It is not yet a physical projection law.

## 5.3 Roster delta

Create a complete transition register:

```text
100-row payload roster
81-row roster
```

For every candidate ID, report:

```text
present in 100
present in 81
removed
reintroduced
newly added
row type
p
g
sign
matter/antimatter/neutral
M_native
qA_source_support
tensor_carrier_support
retained_write_support
source status
user-reclassified status
```

Explicitly test whether the 81 roster:

```text
retains four charged/conjugate p=9,g=0 rows
excludes the neutral p=9,g=0 row
excludes QP093A-0066
excludes QP093A-0069
excludes QP093A-0299
retains QP093A-0051
```

Do not assume these statements. Verify them from the workbooks.

This role-sensitive behavior may be more important than the raw scalar 9.

---

# 6. Phase III — Test the 8 + 1 -> 9 Row Shadow

## 6.1 Same-role arithmetic

For every depth \(g\) and every row role for which all three rows exist, test:

\[
M_{\rm native}(p=8,g,\mathrm{role})
+
M_{\rm native}(p=1,g,\mathrm{role})
=
M_{\rm native}(p=9,g,\mathrm{role}).
\]

Also test the identity separately for:

```text
M_observed_candidate
qA_source_support
tensor_carrier_support
retained_write_support
surface debit/credit
```

Use exact rational arithmetic where possible.

## 6.2 Competing decompositions

The scalar identity \(9=8+1\) is not arithmetically unique.

Test at minimum:

\[
9=6+3.
\]

Compare:

```text
p8 + p1
p6 + p3
```

under the same row laws and depths.

The scientific question is not whether both sums equal 9.

The question is whether the **typed source roles** uniquely privilege the 8-plus-1 path as:

```text
unresolved surface + independent axis channel -> closure witness
```

A full structural interpretation requires source-backed type asymmetry between:

```text
8 and 1
versus
6 and 3
```

If no such source-backed distinction exists, preserve:

```text
ARITHMETIC_IDENTITY_PASS
TYPED_WITNESS_INTERPRETATION_OPEN
```

## 6.3 Witness packet versus witness occurrence

Test whether:

```text
all five p=9,g=0 rows are non-payload
```

or instead:

```text
only the neutral p=9,g=0 occurrence acts as the closure witness,
while charged and conjugate p=9 rows retain matter roles
```

The 100-row and 81-row overlays may be using different projections.

Do not force one rule to govern both unless the sources support it.

Possible outcome:

```text
CR120R packet exclusion = global accounting overlay
F81 projection = role-sensitive matter projection
```

Test this explicitly.

---

# 7. Phase IV — Investigate QP093A-0066

## 7.1 Frozen row

Verify the supplied row directly:

```text
candidate_id                QP093A-0066
route                       neutral_single_write[p=8,g=2]
operator class              OUTER_BINARY_NEUTRAL
partition                   8
closure depth               2
charge                      neutral
M_native                    144
M_observed_candidate        144
qA_source_support           144
tensor_carrier_support      18
retained_write_support      126
matter_row_allowed          yes
promotion                   STABLE_USER_RECLASSIFIED_FROM_RESONANCE
```

## 7.2 Exact identities

Test:

\[
M_{\rm native}=qA=144=N,
\]

\[
\text{tensor support}=18=\Theta,
\]

\[
\text{retained support}=126=M,
\]

\[
144=18+126.
\]

Determine whether this row is:

```text
a physical particle candidate
a unit closure-budget template
a neutral ledger cell
a row-level implementation of N -> Theta + M
a resonance promoted by manual classification
more than one of these under different contexts
```

The answer must be source-typed.

Do not infer physical identity from the numbers alone.

## 7.3 Neighboring triad

Locate and verify the neutral \(g=2\) rows:

```text
p=1,g=2
p=8,g=2
p=9,g=2
```

Expected candidates to verify:

```text
QP093A-0051  p=1,g=2  M_native=18
QP093A-0066  p=8,g=2  M_native=144
QP093A-0069  p=9,g=2  M_native=162
```

Test:

\[
18+144=162,
\]

and the same relation in every support column.

Ask whether the triad is a row-level image of:

\[
\Theta+N=L
\]

as well as:

\[
1+8=9.
\]

This may be the cleanest bridge between the scalar closure hierarchy and the row ledger.

## 7.4 Why QP093A-0066 is absent from the 81 roster

Compare hypotheses:

```text
H66-A:
    QP093A-0066 is excluded because it is a structural N144 template,
    not a retained matter payload row.

H66-B:
    It is excluded because its retained component M126 is already represented
    globally by the 12,600 account.

H66-C:
    It is excluded by an independent geometry or symmetry rule.

H66-D:
    It was omitted manually to reach 81 / 12,600.

H66-E:
    Its exclusion is incidental and has no structural meaning.
```

Use roster features and source provenance, not narrative preference.

---

# 8. Phase V — Investigate QP093A-0299

## 8.1 Frozen row candidate

Verify the supplied source row:

```text
candidate_id                QP093A-0299
route                       closed_scalar_loop[R2_retention_7_over_8]
operator class              CLOSED_SCALAR_LOOP
route class                 closed_loop_parent
partition signature         12+12
closure depth               3
charge                      neutral
spin/hand                   scalar_closed_loop
closure status              CLOSED_LOOP_WRITE
stability status            UNSTABLE_SCALAR_REVEAL_CANDIDATE
M_native                    126000
surface debit               750
M_observed_candidate        125250
qA_source_support           125250
tensor_carrier_support      15656.25
retained_write_support      109593.75
known match                 Higgs (H)|downstream_only
promotion                   MATTER_ROW_ALLOWED_AFTER_REVEAL_DEBIT
stable_particle_flag        false
```

Resolve the workbook-1/workbook-2 discrepancy before interpretation.

## 8.2 Exact identities

Test:

\[
126000-750=125250,
\]

\[
15656.25=\frac18(125250),
\]

\[
109593.75=\frac78(125250),
\]

\[
125250=15656.25+109593.75.
\]

Normalize only for comparison, not to invent a unit conversion:

\[
126000/1000=126,
\]

\[
750/1000=0.75,
\]

\[
125250/1000=125.25.
\]

Determine which operations are source-authorized:

```text
native 126 GeV account
surface debit 0.75 GeV
observable 125.25 GeV reveal
post-reveal 1/8 versus 7/8 support split
```

Do not assume that the 1/8 and 7/8 columns are physical Higgs decay products.

## 8.3 Compare QP093A-0066 and QP093A-0299

Build a typed comparison table:

| Feature | QP093A-0066 | QP093A-0299 |
| --- | --- | --- |
| structural level | row-local neutral single write | closed scalar loop |
| native account | 144 | 126000 |
| pre/post debit | no debit | 126000 -> 125250 |
| tensor fraction | 18 | 15656.25 |
| retained fraction | 126 | 109593.75 |
| stable particle flag | true | false |
| role candidate | N/Theta/M unit cell | Higgs reveal parent |
| included in 81 roster | verify | verify |

Test whether these rows belong to different stages:

```text
0066:
    N144 -> Theta18 + M126
    closure-budget decomposition

0299:
    M126 native scalar account
    -> surface reveal 125.25
    -> downstream support split
```

Do not merge them merely because both contain 126.

## 8.4 Scale coincidence control

Test whether:

\[
12600=\frac{126000}{10}
\]

has any source-backed unit, multiplicity, or projection meaning.

Compare against arbitrary decimal rescalings.

If the factor 10 has no prior source role, classify this as:

```text
NUMERIC_SCALE_COMPATIBILITY_ONLY
```

---

# 9. Phase VI — Discover Candidate 81-Row Projection Rules

This phase is target-aware by design.

It may inspect:

```text
the 81-row membership
the row count
the 12,600 total
all row metadata
```

The purpose is to discover candidate rules, not validate them.

## 9.1 Feature inventory

Use candidate features such as:

```text
p
g
row role
matter / antimatter / neutral
charge sign
q_abs
operator class
route class
closure status
stability status
source versus user reclassification
surface contact class
owner closure
M_native
qA
tensor support
retained support
membership in a complete conjugate orbit
membership in p1/p8/p9 triads
```

## 9.2 Candidate rule families

Search for the smallest understandable rule in these families:

```text
R1 role-sensitive W9 exclusion
R2 complete matter-antimatter orbit retention
R3 neutral fixed-point or template exclusion
R4 remove rows that encode N144 or L162 as structural templates
R5 retain Theta-bearing traffic while excluding duplicated N/L templates
R6 depth-limited finite geometry
R7 face occupancy with one reserved closure address
R8 carrier/container separation
R9 minimum-lift or minimum-connection projection
R10 symmetry quotient under matter-antimatter conjugation
```

A rule may use observed membership during discovery.

Penalize:

```text
candidate-ID whitelists
one-off exceptions
rules that mention 81 or 12,600
rules that directly optimize the target sum
rules that change when one row is perturbed
```

## 9.3 Rule score

Score candidate rules on:

```text
description length
number of exceptions
source-typed features used
symmetry preservation
conjugate-pair preservation
ability to explain QP093A-0066
ability to explain QP093A-0299
ability to explain charged versus neutral p=9 behavior
stability under row-order shuffle
stability under non-target numeric perturbation
```

Output the top candidate rules even if none is ready for promotion.

---

# 10. Phase VII — Freeze and Validate Candidate Rules

After discovery, freeze:

```text
candidate rule text
allowed inputs
forbidden inputs
expected scope
source references
code hash
```

Then validate without using:

```text
current 81-sheet membership
target count 81
target sum 12,600
candidate-ID whitelist
observed binding residuals
```

## Validation sets

Use at least:

```text
the 105-row source lane
the CR120R 100-row overlay
a shuffled-row copy
same-scalar role swaps
p6+p3 versus p8+p1 controls
synthetic missing-conjugate cases
synthetic duplicated-witness cases
a workbook copy with M_native hidden
```

If possible, create a held-out set of row families not used to infer the rule.

## Validation outcomes

```text
PASS_F81_PROJECTION_RULE
    rule independently selects a coherent 81-position structure
    and only after selection lands on the known 12,600 total.

PASS_TYPED_PROJECTION_STRUCTURE_TOTAL_NOT_UNIQUE
    rule is structurally coherent but the exact total is not uniquely implied.

BOUNDARY_F81_M126_WELD_RULE_PARTIAL
    some roster features are explained but exceptions remain.

BOUNDARY_NUMERIC_WELD_DISCOVERY_ONLY
    no observation-blind rule survives.

FAIL_PROJECTION_HYPOTHESIS
    candidate rule fails controls or requires target-aware exceptions.
```

A boundary is an acceptable result.

---

# 11. Phase VIII — Binding Application

Do not insert:

```text
126
144
162
12,600
16,200
```

as new fitted coefficients.

Do not subtract a global ledger total from nuclear mass.

The goal is to determine whether the row findings improve the **construction and fee accounting** used by the existing binding model.

## 11.1 Frozen baseline

Reproduce the current controlling binding baseline from CR274 / CR277.

Record:

```text
training rows
test rows
extended observed rows
current coefficients
current operator strengths
RMS
mean residual
within 5 MeV
within 8 MeV
family residuals
light-nucleus failures
```

Preserve CR249 and CR250:

```text
linear connection-fee sum failed
single global rescale did not repair it
geometry remained necessary
```

## 11.2 Candidate binding consequences

Test these separately.

### B1 — Witness non-double-counting

Do not charge a separate constituent mass or local fee for a row occurrence proven to be only a closure witness.

Test role-sensitive behavior:

```text
neutral W9 witness occurrence
charged p=9 matter occurrence
carrier-9 occurrence
support-9 occurrence
```

Do not remove all numerical nines.

### B2 — N/Theta/M unit-cell constraint

Use QP093A-0066 only as a structural constraint:

\[
N=144=\Theta18+M126.
\]

Ask whether an isotope construction should conserve:

```text
native closure budget
carrier allocation
retained matter allocation
```

Do not make QP093A-0066 an isotope-specific correction.

### B3 — Higgs closed-loop exclusion or global readout

Test whether QP093A-0299 is:

```text
a global scalar reveal row
not an ordinary constituent row
not repeated once per isotope connection
```

Compare binding calculations with accidental inclusion versus typed exclusion.

### B4 — Lift-excess fee

For each sourced row, distinguish:

```text
native mass/account
observed/support account
tensor allocation
retained allocation
surface debit or credit
```

Compare candidate fee definitions without assuming one is correct:

```text
F1 = qA_source_support - M_native
F2 = qA_source_support - retained_write_support
F3 = tensor_carrier_support
F4 = surface debit/credit only
F5 = no direct row fee; geometry and existing operators only
```

Check algebraic dependence. If two candidates are identical by column construction, do not count them as independent models.

Preserve the Theta zero-fee rule where it is already sealed.

### B5 — Geometry-first allocation

Any fee must be multiplied by a count derived from isotope geometry or a source-backed row composition.

Do not infer the number of connections from the observed binding residual.

Compare:

```text
volume contribution
surface exposure
road/crossing contribution
neutron-excess asymmetry
pairing/shell predicates
candidate support-piece inventory
```

## 11.3 Discovery and validation split for binding

### Binding discovery lane

Use only the original binding training set.

Allow candidate feature comparison.

Do not evaluate the held-out rows until the candidate formula is frozen.

### Binding validation lane

Freeze:

```text
feature definitions
coefficients
operator predicates
geometry rule
row inclusion/exclusion rule
```

Then evaluate:

```text
original test set
extended observed rows
full 126-row table
family-specific residuals
```

No same-run repair.

No per-isotope parameters.

No adding a rule because it fixes one named outlier.

## 11.4 Improvement gate

A candidate binding change is useful only if it:

```text
improves held-out RMS or family behavior
does not materially damage existing anchors
reduces a named structural debt
uses fewer or equally many free parameters
survives wrong controls
has an interpretable row/geometry meaning
```

Report negative results.

Possible verdicts:

```text
PASS_BINDING_IMPROVEMENT_FROM_TYPED_LEDGER_RULE
PASS_BINDING_ACCOUNTING_CLEANUP_NO_NUMERIC_IMPROVEMENT
BOUNDARY_BINDING_FEATURE_PROMISING_HOLDOUT_NOT_IMPROVED
FAIL_BINDING_APPLICATION
```

---

# 12. Wrong Controls

At minimum test:

```text
WC1 remove all scalar-9 rows
WC2 treat every p=9 row as W9
WC3 treat p6+p3 as semantically identical to p8+p1
WC4 count W9 as an extra constituent
WC5 count QP093A-0066 once per isotope without geometry
WC6 count QP093A-0299 once per isotope
WC7 use 12600 or 16200 as a fitted coefficient
WC8 use the target total to choose the 81 rows
WC9 use candidate-ID whitelist
WC10 change QP093A-0299 M_native to zero without source authority
WC11 charge Theta a local lift fee
WC12 use all of qA as binding energy
WC13 fit a new coefficient for each isotope family during validation
WC14 repair a held-out failure in the same run
WC15 infer physical particle identity from scalar equality
WC16 use 81-row membership as proof of a literal spatial grid
```

---

# 13. Required Artifacts

Create one campaign folder containing:

```text
SOURCE_MANIFEST.json
WORKBOOK_DIFF.csv
WORKBOOK_DIFF_SUMMARY.md
ROW_SCHEMA_AND_UNITS.md
ROW_OCCURRENCE_REGISTER.csv
ROSTER_100.csv
ROSTER_81.csv
ROSTER_100_TO_81_DELTA.csv

P1_P8_P9_SAME_ROLE_TEST.csv
P6_P3_P9_CONTROL.csv
WITNESS_PACKET_VS_OCCURRENCE_REPORT.md

QP093A_0066_DOSSIER.md
QP093A_0299_DOSSIER.md
QP093A_0051_0066_0069_TRIAD.csv
HIGGS_WORKBOOK_SOURCE_CONFLICT.json

LEDGER_IDENTITIES.json
LEDGER_IDENTITIES.md

F81_DISCOVERY_FEATURES.csv
F81_CANDIDATE_RULES.json
F81_CANDIDATE_RULE_SCORECARD.csv
F81_FROZEN_RULE_CONTRACT.json
F81_VALIDATION_RESULTS.json

BINDING_BASELINE.json
BINDING_FEATURE_DEFINITIONS.json
BINDING_DISCOVERY_RESULTS.csv
BINDING_FROZEN_CANDIDATE.json
BINDING_HOLDOUT_RESULTS.csv
BINDING_MODEL_COMPARISON.md
BINDING_WRONG_CONTROLS.json

ASSUMPTION_REGISTER.json
PRECOMMIT_DISCOVERY.md
PRECOMMIT_VALIDATION.md
runner_discovery.py
runner_validation.py
result.md
summary.json
provenance.json
VALIDATION_REPORT.json
COMMAND_LOG.txt
OPENED_FILE_MANIFEST.json
HASHES.txt
```

---

# 14. Component Findings

Report each separately:

```text
CR120R_100_ROW_L162_REPRODUCTION
    PASS | FAIL

W8_X1_W9_TYPED_INTERPRETATION
    PASS | ARITHMETIC_ONLY | BOUNDARY | FAIL

P9_PACKET_VS_ROLE_OCCURRENCE
    RESOLVED | PARTIAL | OPEN

QP093A_0066_N_THETA_M_ROLE
    PASS | STRUCTURAL_ONLY | BOUNDARY | FAIL

QP093A_0299_HIGGS_ROLE
    PASS | STRUCTURAL_ONLY | SOURCE_CONFLICT | BOUNDARY | FAIL

F81_81_ROW_PROJECTION
    PASS | PARTIAL | DISCOVERY_ONLY | FAIL

BINDING_APPLICATION
    IMPROVED | ACCOUNTING_CLEANUP_ONLY | BOUNDARY | FAIL
```

Do not collapse these into one optimistic verdict.

---

# 15. Primary Verdict Tree

Return one primary verdict.

```text
PASS_QP093A_LEDGER_TO_MATTER_RULE_AND_BINDING_APPLICATION
```

Use only if:

- the 100- and 81-row identities reproduce;
- the W8/X1/W9 mapping is source-typed rather than arithmetic only;
- QP093A-0066 and QP093A-0299 have resolved roles;
- a frozen observation-blind 81-row rule validates;
- the binding application improves held-out performance or closes a named structural debt without new leakage.

```text
PASS_EXACT_LEDGER_IDENTITIES_TYPED_ROW_ROLES_BINDING_APPLICATION_OPEN
```

Use when the accounting and row roles become clear but binding does not improve.

```text
BOUNDARY_LEDGER_IDENTITIES_EXACT_F81_PROJECTION_OR_BINDING_RULE_OPEN
```

Use when the exact totals survive but the projection or binding mechanism remains incomplete.

```text
FAIL_LEDGER_TO_MATTER_INTERPRETATION
```

Use when the observed totals require target-aware editing, unsupported row changes, or fail the controls.

```text
INVALID_SOURCE_CONFLICT
```

Use only if the controlling workbook or source rows cannot be established.

---

# 16. Hard Stops

## STOP 1 — Do not stop because no installed operator already exists

This is a discovery campaign.

Absence of an installed operator is a finding to document, not a reason to refuse to inspect the workbook.

## STOP 2 — Stop before validation if no candidate rule can be frozen

Return the discovery deck and preserve the numeric boundary.

## STOP 3 — Stop a binding candidate if it requires target leakage

Preserve the rest of the campaign.

## STOP 4 — Do not modify sealed source records

Use overlays and new artifacts.

## STOP 5 — Do not install a new SAM Language operator

This campaign may produce a handoff contract only after scientific validation.

---

# 17. Copy-Ready Master Prompt

```text
Execute SAM_QP093A_LEDGER_TO_MATTER_W9_HIGGS_BINDING_DISCOVERY.md exactly as written.

Use GPT-5.6 Sol High.
If unavailable, use GPT-5.5 Extra High.

This is a constructive discovery campaign followed by a frozen validation
campaign.

Do not reject the work merely because a finished F81 projection operator is
not already registered.

Investigate:

    16,200 over 100 QP093A rows
    12,600 over 81 QP093A rows
    the typed 8 + 1 -> 9 closure relation
    QP093A-0066
    QP093A-0299
    possible binding improvements

Preserve CR120R and CR120S.

First audit and diff:

    Updated Particle Rows.xlsx
    Updated Particle Rows2.xlsx

Resolve the QP093A-0299 M_native discrepancy between the two workbooks.

Reproduce:

    105 rows
    minus five p=9,g=0 occurrences
    = 100 rows

    M_native total = 16,200
    mean = 162 = L

Reproduce:

    81-row sheet
    M_native total = 12,600 = 100*126

Test the exact accounting ladder:

    100L = 16,200
    100N = 14,400
    100M = 12,600
    L = N + Theta
    M = N - Theta

Audit the canonical typed hierarchy. Do not merge scalar occurrences.

Distinguish:

    S8 binary surface
    p=8 QP coordinate
    carrier 8
    support 8

    X1 axis channel
    p=1 QP coordinate
    carrier 1
    support 1

    W9 closure witness
    p=9 QP coordinate
    carrier 9
    support 9

Test same-role row arithmetic:

    p8 + p1 = p9

at every available depth and in every support column.

Compare against:

    p6 + p3 = p9

The arithmetic identity alone is not enough. Determine whether source types
uniquely privilege 8 + 1 as unresolved surface plus independent axis channel.

Determine whether the five p=9,g=0 rows form a global accounting packet while
only the neutral p=9,g=0 occurrence acts as the row-level witness in the F81
matter projection.

Create a full 100-to-81 roster delta.

Investigate QP093A-0066:

    p=8,g=2 neutral
    M_native = 144
    qA = 144
    tensor support = 18
    retained support = 126

Test:

    144 = 18 + 126
    N = Theta + M

Locate the p=1,g=2 and p=9,g=2 neutral neighbors and test the expected triad:

    18 + 144 = 162
    Theta + N = L

Investigate why QP093A-0066 is absent from the 81-row roster.

Investigate QP093A-0299:

    closed scalar loop
    M_native = 126000 in the supplied source row
    surface debit = 750
    observed/qA = 125250
    tensor support = 15656.25
    retained support = 109593.75

Test:

    126000 - 750 = 125250
    125250/8 = 15656.25
    7*125250/8 = 109593.75

Determine whether QP093A-0066 is a closure-budget unit cell and QP093A-0299 is
a separate Higgs reveal parent. Do not merge them because both contain 126.

Discovery may use the 81-row membership and totals to infer candidate rules.

After discovery, freeze the best candidate rules.

Validation must not use:

    current membership
    candidate-ID whitelist
    target count 81
    target sum 12,600
    observed binding residuals

For binding, reproduce the frozen CR274/CR277 baseline first.

Do not use 126, 144, 162, 12,600, or 16,200 as fitted coefficients.

Test only typed consequences:

    witness non-double-counting
    N/Theta/M conservation
    Higgs row as global reveal rather than repeated constituent
    lift-excess fee definitions
    geometry-derived support counts
    Theta zero-fee preservation

Use the original binding training lane for discovery.

Freeze any candidate formula before opening held-out and extended rows.

No same-run repair.
No per-isotope parameters.
No target-specific patches.

Generate all required source, roster, row-dossier, candidate-rule, validation,
binding-comparison, provenance, and hash artifacts.

Stop after one complete discovery and one frozen validation.

Return:

    exact meaning supported for 16,200 / 100
    exact meaning supported for 12,600 / 81
    canonical 8 + 1 -> 9 interpretation
    role of QP093A-0066
    role of QP093A-0299
    status of the F81 projection rule
    binding result
    remaining boundaries
    primary verdict
```
