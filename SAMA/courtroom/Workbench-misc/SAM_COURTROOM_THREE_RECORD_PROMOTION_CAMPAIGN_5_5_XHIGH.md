# SAM Courtroom Three-Record Promotion Campaign
## GPT-5.5 Extra High Execution Brief

**Purpose:** Complete three narrowly bounded Courtroom records before beginning SAM Language v0.3.

**Model:** GPT-5.5 Extra High

**Working roots**

```text
COURTROOM_ROOT = C:\VS\The_Courtroom
G_TEST_ROOT    = C:\VS\Stam_model-A-v1.0\tests\Substrate
```

**Required outcome:** Create and validate three new, non-destructive Courtroom records:

1. Higgs direct-weld promotion.
2. CR253 matter-surface semantic clarification.
3. Cosmic-budget typed-readout promotion.

This campaign is not permission to reanalyze the full repository, redesign SAM, repair old records, begin SAM Language v0.3, or generate new forecasts.

---

# 1. Governing Rules

## 1.1 Preserve the existing record

Do not modify, rename, delete, overwrite, or re-seal any existing:

- G test;
- Courtroom record;
- precommit;
- runner;
- result;
- summary;
- hash file;
- appeal;
- supersession record;
- source document;
- SAM Language v0.2 artifact.

Every output from this campaign must be written as a new sibling Courtroom record or a new campaign-level handoff artifact.

## 1.2 Use bounded source sets

For each record, inspect only:

- the primary sources named below;
- directly cited provenance dependencies;
- the branch index needed to select a non-colliding CR number;
- stewardship and hashing utilities required by current Courtroom convention.

Do not crawl unrelated branches.

## 1.3 Precommit before implementation

For each record:

1. Locate and verify the permitted source files.
2. Extract the exact source contracts.
3. Write the precommit.
4. Hash the precommit.
5. Only then write the runner.
6. Hash the runner before execution when required by Courtroom convention.
7. Execute once.
8. Write machine-readable and prose results.
9. Hash the final artifact set.
10. Independently validate the completed folder.

If a source chain is insufficient, return a boundary or invalid-source verdict. Do not invent a bridge.

## 1.4 Exact arithmetic

Use integers, rational arithmetic, and symbolic `pi` wherever possible. Convert to floating point only for declared physical readouts.

Do not use measured target values to select:

- a formula;
- a route;
- a coefficient;
- a normalization;
- a row class;
- a placeholder name.

## 1.5 Preserve verdict discipline

Do not revise a verdict tree after seeing the result.

Do not promote a boundary to a pass because the numerical result looks attractive.

If one record fails or reaches a boundary, preserve it and continue only to the next independent record.

## 1.6 Campaign stop condition

Stop immediately after:

- all three new CR folders are complete;
- every summary JSON parses;
- every generated hash matches;
- a v0.3 handoff manifest is written;
- no existing source artifact was modified.

Do not begin SAM Language v0.3.

---

# 2. Preflight

Before creating any CR:

1. Verify both working roots exist.
2. Record current Git status if Git is available.
3. Record hashes or file metadata for every permitted primary source.
4. Locate the destination branch indexes.
5. Select the next available CR number in each destination branch.
6. Record the proposed destination and why it is canonical.
7. Confirm that no destination folder already exists.

Create:

```text
SAM_THREE_CR_PROMOTION_PREFLIGHT.md
SAM_THREE_CR_SOURCE_MANIFEST.json
```

The source manifest must list every file actually opened.

---

# 3. Record One — Higgs Direct-Weld Promotion

## 3.1 Objective

Promote the completed G748c Higgs direct weld into a canonical Courtroom record.

The record must establish the typed chain:

\[
\text{Resolved Higgs ontology}
\rightarrow
q_H=3
\rightarrow
r_{\mathrm{bounce},H}=\frac{1}{64\pi}
\rightarrow
q_{A,H}=m_H(1+r_{\mathrm{bounce},H})
\rightarrow
\text{Higgs A-source readout}.
\]

This is a promotion and exact replay record, not a new formula search.

## 3.2 Primary source set

Required sources:

```text
G439_HEAVY_ELECTROWEAK_TOP_SLOT_SELECTOR
G444_WRITE_SECTOR_Q_SLOT_DERIVATION
G744c_Q_A_SOURCE_STRENGTH_BRIDGE
G745c_HIGGS_NINE_SIXTEENTHS_SCALAR_ROUTE
G748c_HIGGS_DIRECT_WELD
CR120_QP091_HIGGS_EW_PRECISION_ITERATIVE_REFINEMENT_INTAKE
```

Use additional files only when directly required by those sources, including:

- G231/G231B for ontology provenance if cited;
- CR100a or CR103b for the neighboring \(q_A\) bridge if cited;
- CR104c for the scalar route if cited;
- CR103a/CR104a only if the direct chain explicitly depends on them.

Do not infer the weld from unrelated Higgs or electroweak work.

## 3.3 Locked source contracts

Freeze:

```text
Higgs structural role:
    q_H = 3

Higgs bounce ratio:
    r_bounce,H = 1/(64*pi)

General source-strength bridge:
    q_A = m * (1 + r_bounce)

Expected dimensionless ratio:
    q_A,H / m_H = 1 + 1/(64*pi)
```

If the replay uses a Higgs mass input, identify its exact provenance. Do not alter that input to improve the readout.

## 3.4 Required typed outputs

```text
ResolvedHiggsASourceOntology
HiggsQSlotRole[q=3]
HiggsBounceRatio[1/(64*pi)]
HiggsASourceReadout[q_A,H]
HiggsASourceRatio[q_A,H/m_H]
```

## 3.5 Required wrong controls

At minimum:

1. Replace \(q_H=3\) with a neighboring noncanonical q-slot.
2. Remove the bounce term.
3. Replace \(1/(64\pi)\) with a sourced wrong-route control.
4. Apply the neighboring particle bridge through an unauthorized Higgs channel.
5. Attempt to construct \(q_{A,H}\) from an observed residual.

The canonical route must satisfy its precommitted conditions while the controls fail the relevant guards.

## 3.6 Permitted verdicts

Return exactly one:

```text
PASS_HIGGS_DIRECT_WELD_PROMOTION
BOUNDARY_SOURCE_CHAIN_INCOMPLETE
FAIL_HIGGS_DIRECT_WELD_REPLAY
INVALID_HIGGS_PROVENANCE
```

## 3.7 Required artifacts

```text
CRxxx_PRECOMMIT.md
CRxxx_runner.py
CRxxx_result.md
CRxxx_summary.json
CRxxx_provenance.json
CRxxx_typed_contract.json
HASHES.txt
```

The typed contract must be sufficient for later registration in SAM Language v0.3 without re-reading the full CR.

---

# 4. Record Two — CR253 Matter-Surface Semantic Clarification

## 4.1 Objective

Clarify and seal what CR253 actually produces.

The new record must not claim that every promoted row is an experimentally identified, independently real, permanently stable particle.

The intended executable chain is:

\[
\text{QP093A finite candidate catalog}
\rightarrow
\text{CR253 compatibility/promoter rules}
\rightarrow
\text{80-row structurally stable matter surface}
\rightarrow
\text{controlled naming and placeholder addressing}.
\]

The language-level distinction must be explicit:

\[
\text{StructurallyStableMatterRow}
\neq
\text{ExperimentallyIdentifiedParticle}.
\]

## 4.2 Primary source set

Required sources:

```text
QP093A stable-particle-combination enumerator source and output
CR119, including the recorded QP093A boundary
CR253 and every file required to replay its promoter
CR254 compact charged-row law
CR255 compact neutral-row law
CR256 conjugation/A-operator
CR257 only if CR253 directly depends on its depth-one compact form
SAM Volume II matter discussion of the 80-row surface and naming discipline
```

Use CR233 only if needed to preserve carrier-role typing.

Use CR262/CR269 only if required to distinguish the nuclear carrier/container cipher from the CR253 row promoter. Do not merge those functions.

## 4.3 Required semantic type

Emit a type such as:

```text
TensorCompatibleStableMatterSurface
```

with row-level subtype:

```text
StructurallyStableMatterRow
```

The exact identifier may follow existing conventions, but it must not be the unqualified `StablePhysicalParticle`.

## 4.4 Required replay invariants

Verify the source result exactly:

```text
total promoted rows = 80
matter rows         = 48
antimatter rows     = 32
conjugate parity    = source-defined exact count
row laws            = source-defined exact rational closure
```

Do not change CR253's original verdict or grade.

If CR253 retained a boundary because controls were insensitive or subsumed, preserve that fact.

## 4.5 Naming discipline

Only rows meeting an existing exact naming rule may receive a conventional particle name.

Every other promoted row must receive a deterministic, non-physical placeholder identifier derived only from its structural address, for example:

```text
SAM[p=6,sign=-,depth=0,class=charged_matter]
SAM[p=9,sign=0,depth=1,class=neutral_candidate]
```

The final placeholder format must be:

- deterministic;
- collision-free;
- reversible to the source row;
- free of unsupported PDG names;
- free of implied mass or lifetime claims;
- stable across reruns.

Preserve only source-supported labels, such as:

```text
EXACT_CONTACT
SECTOR_HINT
MIRROR
NEUTRAL_ANCHOR
SUBSTRATE_CANDIDATE
INDEPENDENT_EXISTENCE_UNRESOLVED
```

Do not invent categories merely to fill every field.

## 4.6 Required guards

Reject:

1. naming every promoted row as a known particle;
2. treating tensor compatibility as proof of experimental existence;
3. treating a placeholder as a PDG identity;
4. using PDG membership to decide promotion;
5. changing the 80/48/32 census;
6. applying the Be-8 nuclear stability cipher as the CR253 particle-row selector unless explicitly required by source.

## 4.7 Permitted verdicts

Return exactly one:

```text
PASS_CR253_SEMANTIC_CLARIFICATION
BOUNDARY_CR253_NAMING_CONTRACT_INCOMPLETE
FAIL_CR253_REPLAY_OR_CENSUS
INVALID_CR253_SOURCE_CHAIN
```

## 4.8 Required artifacts

```text
CRxxx_PRECOMMIT.md
CRxxx_runner.py
CRxxx_result.md
CRxxx_summary.json
CRxxx_provenance.json
CRxxx_row_contract.schema.json
CRxxx_promoted_rows.json
CRxxx_placeholder_map.csv
HASHES.txt
```

The row JSON must retain:

- original row address;
- matter/antimatter class;
- charge/sign/depth fields;
- source rule;
- placeholder ID;
- exact-contact status;
- conventional name only when source-authorized;
- independent-existence status.

---

# 5. Record Three — Cosmic-Budget Typed-Readout Promotion

## 5.1 Objective

Seal the complete typed chain that produces:

1. the dimensionless SAM cosmic inventory;
2. the physical baryon-density readout \(\omega_b=\Omega_bh^2\);
3. the derived \(h^2\) and \(H_0\) readouts where supported.

Use Volume I as the canonical conceptual map, then trace every equation to sealed Courtroom authority.

## 5.2 Primary source set

Required sources:

```text
SAM Volume I, Cosmic Budget section and glossary
CR003@19 if required for A0 provenance
CR018 and its appeals or supersessions
CR114 baryon/cosmic-carrier bridge
CR117 CMB scope boundary
CR036@19 eta and H0 cascade
any direct sealed dependency supplying K(T_CMB)
```

Use CR037A only if needed to distinguish later perturbation outputs from the core budget. Do not expand this record into a CMB-spectrum campaign.

## 5.3 Structural inventory contract

Freeze and verify:

\[
A_0=\frac{1}{\pi R},
\]

\[
\chi=\frac{S}{D}A_0,
\]

\[
\Omega_b=\hat hA_0(1-\chi),
\]

\[
\Omega_m=RA_0=\frac{1}{\pi},
\]

\[
\Omega_c=\Omega_m-\Omega_b,
\]

\[
\Omega_\Lambda=1-\Omega_m.
\]

Keep the directional refinement as a separate typed output:

\[
\Omega_{m,\mathrm{eff}}
=
RA_0-2A_0(\chi+D\chi^2).
\]

Do not replace the clean \(\Omega_m=1/\pi\) spine with the refinement.

## 5.4 Physical-density contract

Where sealed source authority supports it, verify:

\[
\eta
=
\frac{M}{\hat h^2\Theta}A_0^6,
\]

\[
\omega_b
=
\Omega_bh^2
=
\frac{\eta}{K(T_{\mathrm{CMB}})},
\]

\[
h^2=\frac{\omega_b}{\Omega_b},
\]

\[
H_0=100h\ \mathrm{km\,s^{-1}\,Mpc^{-1}}.
\]

The runner must identify:

- the exact source of \(K\);
- the exact dimensional anchor, if any;
- the exact source of \(T_{\mathrm{CMB}}\);
- which values are SAM outputs;
- which values are external comparators.

Planck or other observational values must never be used to choose or normalize the SAM result.

## 5.5 Required typed outputs

```text
SubstrateFloor[A0]
HorizonQuotient[chi]
BaryonInventoryFraction[Omega_b]
MatterInventoryFraction[Omega_m]
DarkInventoryFraction[Omega_c]
SubstrateVacuumFraction[Omega_Lambda]
EffectiveMatterFraction[Omega_m_eff]
BaryonPhotonRatio[eta]
PhysicalBaryonDensity[omega_b = Omega_b*h^2]
ReducedHubbleParameterSquared[h^2]
HubbleReadout[H0]
```

If the sealed chain does not support one of the later outputs, return a boundary for that sub-contract without invalidating the supported structural inventory.

## 5.6 Required wrong controls

At minimum:

1. Set \(A_0=0\).
2. Remove the \((1-\chi)\) factor from \(\Omega_b\).
3. Replace \(R\) or \(D\) with a noncanonical control.
4. Use \(\Omega_{m,\mathrm{eff}}\) in place of the clean \(\Omega_m\) spine.
5. Use the Planck \(\omega_b\) comparator as an input.
6. Replace the sourced \(K(T_{\mathrm{CMB}})\) with an unsourced normalization.
7. Confuse \(\Omega_b\) with \(\omega_b=\Omega_bh^2\).

## 5.7 Permitted verdicts

Return one primary verdict:

```text
PASS_COSMIC_BUDGET_TYPED_READOUT
BOUNDARY_PHYSICAL_DENSITY_SOURCE_INCOMPLETE
FAIL_COSMIC_BUDGET_IDENTITY
INVALID_COSMIC_BUDGET_PROVENANCE
```

Optional sub-verdicts:

```text
STRUCTURAL_INVENTORY_PASS
PHYSICAL_DENSITY_PASS
HUBBLE_CASCADE_PASS
DIRECTIONAL_REFINEMENT_PASS
```

## 5.8 Required artifacts

```text
CRxxx_PRECOMMIT.md
CRxxx_runner.py
CRxxx_result.md
CRxxx_summary.json
CRxxx_provenance.json
CRxxx_typed_contract.json
CRxxx_dependency_graph.json
CRxxx_values.csv
HASHES.txt
```

The dependency graph must preserve the two chains:

```text
A0 -> chi -> Omega_b
A0 * R -> Omega_m -> Omega_c, Omega_Lambda
A0, chi, D -> Omega_m_eff

A0, M, Theta -> eta
eta, K(T_CMB) -> omega_b
omega_b, Omega_b -> h^2
h -> H0
```

---

# 6. Campaign-Level Validation

After all three records finish:

1. Recompute all generated hashes.
2. Confirm every JSON artifact parses.
3. Confirm no source file changed.
4. Confirm each runner replays from its own folder.
5. Confirm every precommit was sealed before its runner.
6. Confirm no observational comparator entered a derivation path.
7. Confirm no forecast was generated.
8. Confirm SAM Language v0.2 remains untouched.

Create:

```text
SAM_THREE_CR_PROMOTION_RESULT.md
SAM_THREE_CR_PROMOTION_SUMMARY.json
SAM_LANGUAGE_V0_3_HANDOFF.md
SAM_LANGUAGE_V0_3_CONTRACT_MANIFEST.json
HASHES.txt
```

The v0.3 handoff may contain only:

- new CR paths;
- verdicts;
- typed contracts;
- provenance dependencies;
- unresolved edges;
- exact registration recommendations.

Do not modify the language itself.

---

# 7. Final Response Format

Return:

```text
Campaign verdict:

CR 1 Higgs promotion:
CR 2 CR253 clarification:
CR 3 cosmic-budget readout:

New CR folders:
- ...
- ...
- ...

Validation:
- precommits hashed:
- runners executed:
- JSON parsed:
- hashes verified:
- existing artifacts modified: false
- SAM Language v0.2 modified: false
- forecasts generated: false

Remaining named opens:
- ...

STOPPED AS INSTRUCTED
```

---

# 8. Copy-Paste Master Instruction

```text
Execute SAM_COURTROOM_THREE_RECORD_PROMOTION_CAMPAIGN_5_5_XHIGH.md exactly as written.

This is a bounded Courtroom promotion job with three independent records:

1. Promote G748c Higgs direct weld.
2. Clarify CR253 as an 80-row structurally stable/tensor-compatible matter surface with controlled conventional naming and deterministic placeholders.
3. Promote the complete typed cosmic-budget readout from A0 through Omega_b and Omega_m and, where sealed, through eta, omega_b, h^2, and H0.

Do not reanalyze the full repository.
Do not modify existing G tests, CRs, appeals, source documents, or SAM Language v0.2.
Do not begin SAM Language v0.3.
Do not generate forecasts.
Do not invent a missing bridge.
Precommit and hash each record before writing its runner.
Use exact arithmetic where possible.
Preserve every boundary or failure.
After all three records validate, write the v0.3 handoff manifest and stop.
```
