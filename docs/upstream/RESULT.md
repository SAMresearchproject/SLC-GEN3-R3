# SLC-GEN2-R4 installed completion

**Status: INSTALLED_AND_ADOPTED — PASS. Date: 2026-09-07.**

**SLC-GEN2-R4** is the sole global SLC and **SLC-GEN2-CEV1-R4** is its CE
facade, build **GEN2-REUSE-INFO1-20260907**, generation
**GEN2-REUSE-INFO1-20260907-G1**. The cumulative release retains all R2/R3
capabilities and their exact native sources. It exposes **27 capabilities and
38 GEN2 operations**; installation authenticates **339 files**, and all seven
current links resolve into `CURRENT_REVISION`. The
[activation receipt](installation/run001/ACTIVATION.json) preserves the R3
preimages and records the installed verification. [Inheritance](INHERITANCE.json)
and the [interface specification](INTERFACE_SPEC.md) describe the cumulative
route. Activation authority is H001013.

The approved upgrade is implemented, qualified, promoted, installed and exercised
through the appropriate managed programs. RH, ATOM3D/A3D4, Starbreaker, matter,
reopened MP and tau keep their source models and scoped application operations.
The native HFM5 identity in a preserved component receipt remains that
component's identity; the
[default-packet adoption receipt](hardware/default_packet_custody_run001/DEFAULT_PACKET_ADOPTION.json)
binds its execution and automatic R4 summaries to the installed global SLC and CE.

## Qualification and installed use

| Evidence | Completed result |
|---|---|
| [Cumulative qualification](qualification/run003/RESULT.json) | **570,468 assertions PASS**: 569,339 inherited R2/R3; 987 independent arithmetic/history; 34 core; 41 observation/policy; 29 boundary; 38 integration. All 100 candidate source files remained unchanged; 35 exact integration fixtures are retained. |
| [Installed adoption](adoption/run001/RESULT.json) | **286 exact comparisons**, eight routes and **35 fresh CE CLI processes**. Routes: global Python, ATOM3D, Starbreaker, matter, RH domain, MP, CE CLI and RH program. |
| [Project-session adoption](project_adoption/run001/RESULT.json) | **Seven actual calls** across RH, A3D4, SB, Mersenne-prime, matter and TAU project routes, including two fresh CLI processes. Announcements and call receipts identify R4 and the selected domain engine. |
| [Current-entry guards](guards/run001/RESULT.json) | **14 checks PASS**, one global SLC, 339 authenticated files and all seven current links. Guard fixtures preserved live files. |
| [Installed native hardware](hardware/native_run001/RESULT.json) | All **49,536 packed values** across 384 rows agree on H14F, Ryzen and actual 780M execution; 52 motion transitions are checked against that native batch. Packed counts include addresses and padding. |
| [Ordinary ATOM3D execution](DEFAULT_PACKET_STDOUT.json) | All **113,664 exact receiver values** agree across CPU/GPU, retaining 4,736 source tasks and 14,208 T18 Writes. Automatic attachment contains **832 distinct source histories and 2,496 role history summaries**, with no motion-enabling flags. |

The independent arithmetic/history checks cover 196 rational arithmetic cases
and 27 action traces using independent factor/product rise, fall and maximum
oracles. Inherited comparison preserves explicit R3 readout choices where R4's
authorized automatic selection rule changes the selected detail.

## What R4 now computes

Signed arithmetic represents exact rational values with native HD signatures,
retains every contribution edge and computes `+`, `-`, `*` and `/` with explicit
sign, zero and division-by-zero outcomes. Examples retain `9=8+1`, produce the
new exact coordinate `ln(5)` from `2+3`, and preserve both occurrences in `x-x`.
Quantity/reference compatibility allows contributions from different owners to
combine when their declared meanings agree. Referenced energy 6 with reference 2,
multiplied by scalar 2, returns value 12, reference 2 and `ln(6)`.
[Core specification and evidence](CORE_SPEC.md).

Observation selection now distinguishes the raw channel from its returned
resolution. In the four-history barrier example, automatic SCALE
ABOVE/SAME/BELOW supplies `ln(2)` target information and retains two complete
histories; asking for full original HISTORY selects four-valued detail.
The ratio example's adaptive policy supplies `(3/2)ln(2)` with three report
transcripts and expected `3/2` reads/Writes: one branch stops with two histories
and a known target, while the other takes another sign report. A separate native
lookahead case has zero first-step information and `ln(2)` over two steps.
All target-information ties and original weighted histories remain retained.
[Exact observation findings](observation_focus/20260907T083012460303Z/RESULT.json),
[selection and policy semantics](OBSERVATION_SPEC.md).

Construction-boundary information uses one declared exact measure on linked
source alternatives. The four-history aggregate exterior carries
`(3/2)ln(2)` and retains two owner arrangements in its middle report; separate
ports supply the remaining `(1/2)ln(2)`. A shared-coordinate join admits two
combinations and yields `ln(2)` between component states. Li-6 site/cycle and
calibrated encounter cases retain their construction associations and resolve
the supplied alternatives. [Boundary evidence](boundary_tests/final002/RESULT.json)
and [source/measure contract](BOUNDARY_SPEC.md).

For positive source action, ordinary motion now carries the owner's exact
`U=sum max(0,delta_log)`, `D=sum max(0,-delta_log)`, `V=U+D`, and
`M=max ln(E_t/E_initial)`, including all maximizing ties. It retains
`U-D=ln(E_final/E_initial)`. The loop `6→8→6` has
`U=D=M=ln(4/3)`, `V=2ln(4/3)` and endpoint log zero. Appending or composing
at the same declared boundary produces the same full summary.
[Focused core result](core_tests/run_20260907T082513646857Z/RESULT.json).

## Reuse and restart

Reuse is measured outside mathematical identities and checkpoint seals.
The focused motion continuation annotates seven new states, reuses two prior
states and validates one shared boundary, with zero full-history annotations.
Its three roles summarize 21 new edges and reuse three earlier edges.
Boundary qualification records 14 appended transition lookups and zero prior
native transition reexecutions. These are actual work counters in the
[core](core_tests/run_20260907T082513646857Z/RESULT.json) and
[boundary](boundary_tests/final002/RESULT.json) records.

The same automatic two-step policy takes **51.022 seconds cold and 1.351 seconds
on repeat**, about **37.8× faster**, with identical exact output. The repeat
adds a sealed-tree cache hit without new policy-node misses. Exact STOP bounds,
profile reuse and complete tree reuse preserve every information tie; the timing
belongs to this recorded four-candidate workload.
[Timing and counters](observation_focus/20260907T083012460303Z/RESULT.json).

All five checkpoint packages pass complete T500 write/readback, returned-byte
equality and restoration through a fresh installed process:

| Checkpoint | Continuation evidence |
|---|---|
| [Motion](hardware/motion_checkpoint_run001/RESULT.json) | 47 new Writes/states; two prior states reused; 141 new role edges and three prior edges reused. Fresh restore separately validates two earlier states and three role edges. |
| [Signed arithmetic](hardware/signed_checkpoint_run001/RESULT.json) | Five new nodes evaluated, five prior nodes reused; five prior nodes semantically validated on fresh restore. |
| [History summary](hardware/history_checkpoint_run001/RESULT.json) | One new edge summarized and one prior edge reused; one prior edge semantically validated on fresh restore. |
| [Adaptive policy](hardware/policy_checkpoint_run001/RESULT.json) | Full policy/target/weight/history result restores identically, retaining its selected continuations and exact alternatives. |
| [Construction boundary](hardware/boundary_checkpoint_run001/RESULT.json) | Complete conditioned/appended relation restores identically; journal validation uses two transition lookups and reexecutes zero prior native transitions. |

The [ordinary packet custody supplement](hardware/default_packet_custody_run001/RESULT.json)
also passes. It transfers **97,488,336 bytes in 16 files**; its manifest covers
**97,486,090 payload bytes in 15 files**. Full returned bytes are identical.
The supplement links the source packet, installed bindings and all automatic
summaries to **34,882,374 bytes of resident native artifacts** on the verified
Crucial T500. Custody performs no additional native mathematical execution.

## Preserved faults, corrections and meanings

The record preserves the executed
[signed-reference propagation fault](core_tests/reference_propagation_fix1/BEFORE.json),
its original source, the [correction](core_tests/reference_propagation_fix1/AFTER.json)
and the passing retest. The [core review](CORE_REVIEW.md) separately records
the review-detected history restart-admission mismatch and its shared admission
fix; that finding had no pre-fix executed failure or source snapshot.

Harness faults remain preserved: the [initial import](core_tests/IMPORT_FAILURE.json),
[floating telemetry serialization](runs/inherited_20260907T081821768122Z/FAILURE.json),
[legacy wrapper delegation](runs/inherited_20260907T081924473451Z/FAILURE.json),
[relative path assembly](qualification/run001/FAILURE.json) and
[fixture-name assembly](qualification/run002/FAILURE.json). Corrected runs
supplement them. These records describe implementation/execution faults.

Automatic approval review initially rejected the
[native payload](hardware/AUTOMATIC_REVIEW_REJECTION_001.json),
[five checkpoint packages](hardware/AUTOMATIC_REVIEW_REJECTION_002.json) and
[default packet supplement](hardware/AUTOMATIC_REVIEW_REJECTION_003.json),
requesting exact payload/destination authorization. Each rejection records that
no process started and that Sean subsequently approved the disclosed transfer.
The [remote authorization](hardware/REMOTE_AUTHORIZATION.json) and passing
custody results preserve execution after those approvals.

The implemented policy horizon is zero, one or two steps on declared source
channels and exact positive weights. A determined original target may retain
multiple histories. Missing observations remain distinct from observed zero;
source-unavailable probes preserve the relation. Dimensional logarithms require
compatible positive references; nonpositive or absent action creates an explicit
summary gap while retaining phase/history. Ordinary deterministic construction
registers its boundary and requests an explicit complete relation and measure
before information is assigned. Source action, normalized sphere coordinates,
probability weights and physical measurements keep their declared meanings.
The current ATOM3D output leaves physical energy scale and isotope incidence
open, and the installed tau selection remains PILOT3.

Helpful information disclosed: resolving the chosen target can require less
reported detail than identifying every history; incremental reuse preserves
those remaining histories and their future native continuations. The report
records the implementation corrections and all completion evidence above.

NEGATIVE DRIFT CHECK — COMPLETION: CLEAR.

The final [current-label correction](installation/final_labels/RESULT.json) also
updates RH’s frontier introduction to the sole R4 SLC/CE and authenticates the
new documentation binding. It preserves the scientific frontier and native code;
its predecessor records retain the registry state captured by the hardware receipts.
Fresh verification still authenticates 339 files. Completion authority: H001014.
