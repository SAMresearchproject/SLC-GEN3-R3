# Native response distinguishes the retained Li-6 constructions

Direction: Sean Brady. Technical map and derivation: Codex. Source: H000976,
the installed A3D41 Li-6 grammar on SLC-GEN2-R4.2.

The retained family is

`z=(t,t,-t,-t,-t,t,t,t,alpha)`, with `t,alpha` in `{1,i,-1,-i}`.

Both motif covers and all six center placements give observed source action
`737/16`. This trial asks what additional information their native responses
carry. All perturbation results here use `rho=1`, the connected native mask,
circulation receiver and active hidden loop.

## Actual native calculation

For each of sixteen selected addresses, `T18_WORD` computes all eighteen
signed quarter Writes. No Python transition calculation supplies these
addresses. There are 288 retained transition histories and 272 distinct
addresses including the selected states. `GEN2_BOUNDARY_OPEN` with the LI6
source compiles each of twelve constructions and evaluates those addresses:
3,264 new exact construction responses, with signed site and cycle currents.

For each original state and construction, `GEN2_SIGNED_LOG` computes

`delta(e,sign)=A(W(e,sign) z)-A(z)`.

This produces 3,456 response differences in twelve complete arithmetic DAGs.
Their leaves cite the native construction receipts. The arithmetic acts on
exact numerical coordinates in candidate source-action units; these numbers
have no assigned physical energy conversion. Independent Python Fraction
subtractions check the native arithmetic and are identified separately.

Each probe starts from the **same restored initial configuration**. A tuple
of probes means several independent responses; it is not a sequence whose
second Write starts at the first Write's endpoint. The retained T18 source
and receiver names are custody identities, with no physical receiver site
introduced by this campaign.

`GEN2_CUSTODY` constructs complete finite inverse fibers and exact entropy.
`GEN2_FORMAL_LOG` compares the single-probe entropies exactly, retaining all
ties. The uniform finite source measure is an information-accounting choice;
source inventory amounts are not population probabilities.

## What the native inverse learns

At known source address 14336, a single Write on edge index 1 (`EDGE_002`)
or index 4 (`EDGE_006`), of either sign, distinguishes all twelve cover and
placement alternatives. These are all four optimal single-probe ties at
that known address. The baseline source-action scalar distinguishes none.

With the starting history unknown, all eighteen independent scalar responses
partition the 192 configurations into 96 pairs. Every pair has identical
cover and placement, with `t -> -t` and `alpha -> -alpha`.

This remaining ambiguity has a direct algebraic explanation. Every source
action is quadratic in the real and imaginary edge currents, so
`A(-z)=A(z)`. A quarter Write commutes with global reversal:
`W(e,sign)(-z)=-W(e,sign)z`. Therefore every scalar response difference has
the same value on a reversal pair. This argument applies to this quadratic
response family; signed current readouts retain the missing orientation.

All 153 unordered pairs from the eighteen signed probes were tested through
GEN2. Eight pairs give the same 96 two-member fibers as the full response
set. Their edge-index/sign patterns are:

`(2,+;5,-)`, `(2,+;6,-)`, `(2,-;5,+)`, `(2,-;6,+)`,
`(3,+;5,-)`, `(3,+;6,-)`, `(3,-;5,+)`, `(3,-;6,+)`.

Every single action probe has at most 24 outputs on the unknown 192-source
roster. Thus two is the minimum number of action probes in this declared
single-Write scalar roster that reaches the full 96-class distinction. All
153 pairs were searched; there is no claim about untested compound operators.

The selected compact pair is `EDGE_003 +1` and `EDGE_007 -1`. The signed site
current at N01 is `alpha`; `GEN2_READOUT` evaluates the linear polynomial

`b = Re(alpha) + Im(alpha)`.

On the admitted cardinal phases, `b` is exactly +1 or -1 and reverses sign
under the remaining global reversal. Two action differences plus this one
signed bit recover the full starting history, cover and placement across
all 192 configurations. This is a phase-plane readout in the source's
existing coordinates; no spatial spin axis or physical magnetic coefficient
is assigned to it.

## Continuation

The new usable object is a native response-based decoder for the tied source
family, with a minimal action-probe pair in the declared roster. It gives the
physical-readout continuation a way to distinguish retained constructions
and source orientation. Extending the decoder across rho and binding its
signed operators to spin, magnetic/quadrupole coefficients and physical
energy conversion are subsequent source-mapping tasks.

**The test result suggests strong contact with the concept.** The tested
concept is exact native response-based distinction and recovery of this
retained Li-6 construction family.
