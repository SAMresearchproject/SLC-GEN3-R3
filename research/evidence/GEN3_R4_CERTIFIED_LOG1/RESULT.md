# GEN3-R4 installed

Date: 2026-09-15. Authority: H001453.

The sole global engine is **SLC-GEN3-R4**, with **SLC-GEN3-CEV1-R4**,
build **GEN3-CERTIFIEDLOG1-20260915**, generation
**GEN3-CERTIFIEDLOG1-20260915-G1**. The owner authorized building, promotion and
full installation. Astra at high reasoning effort implemented the native core,
independent oracle and promotion tooling; the primary Codex agent integrated,
qualified and installed the successor.

## Installed behavior

The shared native C++/GMP worker now provides registered-kernel spectral admission,
certified scalar log1p enclosures, source-sensitive spectral logarithms, exact
derivative/coefficient intervals, adaptive mode refinement, exact-total signed
transport, and source-bound checkpoint export. Source histories, signed moments,
units, reference and ordered coordinates remain attached to results. Each source
is retained once and later readouts link to it. [Contracts](CERTIFIED_LOG.md).

The six calls are `GEN3_SPECTRAL_ADMIT`, `GEN3_LOG1P_ENCLOSURE`,
`GEN3_SPECTRAL_LOG_PROFILE`, `GEN3_SPECTRAL_REFINE`,
`GEN3_SIGNED_TRANSPORT_LOG`, and `GEN3_SPECTRAL_EXPORT`. ATOM3D aliases are
installed. The source provider is MEAN_CUT_V1, dimensions1..65536 and retained
degree at most256; full spectra fit this route through dimension257. The existing
small-matrix operator-log interfaces remain available with their original scope.

Numerical certificates include both omitted-mode and log-evaluation error.
Budget-limited results retain valid intervals and explicitly report unmet accuracy.
Refinement projects only missing source modes and reports rebuilt basis work
separately. Exact-total transport combines signed components before taking logs.

## Executed qualification and adoption

| Evidence | Outcome |
|---|---|
| Newly authored independent reference | 39,132 scoped assertions,96 synthetic cases, dimensions1..8 |
| Native core smoke | 523 native calls,6,782 scoped checks |
| Final integrated candidate | 5,791 readout/admission/recovery checks plus4,032 independent native-source assertions |
| Six frozen kernel anchors | Source hashes, every retained signed moment, energy/tail and direct energy match |
| T500 fixture reader | Exact selected A3D41 archive member recovered; scan-budget handling passes |
| Installed adoption | 57 checks: actual project/CLI/CE execution, preserved operator fixtures, migration, model/source preservation and domain bindings |
| MATTER_SEARCH compatibility application | Current R4 binding and all six preserved input packages verify |
| Final selected registry | PASS:439 authenticated files,2 engines,5 domains,1 global SLC |

Counts retain their distinct scopes; they are not independent research sample
counts. The supplied handoff's companion reference executable was unavailable;
`reference_checks.py` is a separately authored oracle and makes no claim to have
run the handoff's reported17,132 assertions.

The installed project session is recorded in `SESSION.json`. `RECOVERY.json`
records a separate process that exported three exact source/readout/history
records with unchanged native counters, then refined the recovered partial
profile using its three retained modes. `CLI_EXECUTION.json` records the actual
public CLI calculation. Existing `./r3` and `./gen3` commands select R4.

## Measured cost and routing

For the frozen64-coordinate alternating source, median three-run full-pipeline
times were approximately0.186ms direct scalar,2.445ms fixed full spectral scalar,
5.853ms adaptive scalar,5.160ms full spectral profile and11.052ms adaptive profile.
All matched their declared scalar ratio or numerical accuracy requirements.
That source required all64 modes at the chosen tolerance. The comparison therefore
supports retaining the direct route for one-off scalar queries. It does not assign
a general acceleration to adaptive projection.

The separate recovered-mode profile measurements were approximately4.712ms with
a cold worker and3.705ms with a warm worker. A new parameter query on an existing
record requires zero new source projections. The basis may be reconstructed for
extension and is counted. Benchmark contracts and raw measurements are retained
in `BENCHMARK_CONTRACT.json` and `BENCHMARK.json`; full recovery/export timings
are in `RECOVERY.json`. Dispatch remains explicit/deterministic; no model was fitted.

## T500 reuse

The installed `gen3/capabilities/archive_fixture.py` helper retrieves selected
native object hashes under explicit scan/object limits. It retains missing
dependencies and requires semantic admission before engine use. Qualification
uses the exact A3D41 construction record read from the200GiB T500 archive in the
preceding review, retaining its original member hash/root in `T500_FIXTURE.json`.
It does not restore the full archive or transfer the kernel's spectrum to an
unrelated A3D41 exchange operator. Frozen fixtures remain outside fitting.

## Promotion, correction and preservation

Promotion changed45 files through a serialized activation marker with exact
preimages. R3 release records remain immutable; new R4 release records are selected.
Models, source memories, original roots, owner pauses, resource limits and existing
operator behavior are preserved. Predecessor checkpoints migrate through their
authenticated implementation binding. Start fresh project sessions for R4 and
carry intended saved checkpoints through that migration route.

The first registry-valid activation exposed a stale R3 version check in the
inherited GEN2 runtime during actual adoption. The real rollback restored all434
predecessor bindings. The corrected promotion updated that compatibility identity
and passed the full installed adoption. Both attempts and the failure remain
under `activation_attempt1/` and `ADOPTION_FAILURE_*.json`. The MATTER_SEARCH
compatibility application's older generation pin was also carried forward.

`install.py`, `rollback.py`, `BEFORE.json`, `STAGED.json`, `INSTALLED.json` and
the predecessor files preserve the installation transaction. No new training,
research producer, backup timer or remote shutdown was started. This result is a
capability installation; prior domain mathematical outcomes retain their scope.

ORIGINATOR / CONCEPTUAL DIRECTOR: Sean Brady.

AI RESEARCH COLLABORATORS: OpenAI ChatGPT and Codex.

Sean supplied direction; ChatGPT supplied the longer-history design; Codex supplied
the technical review, implementation, independent controls, integration and
qualification. Native GEN3 executed the recorded arithmetic.
