# Installed R4 source operators, creation and terminal exchange

Status: INSTALLED, adoption PASS. Date: 2026-09-16. Authority: H001469.
Sole global SLC: SLC-GEN3-R4. CE: SLC-GEN3-CEV1-R4.
Build: GEN3-SOURCEOPERATORS1-20260916; generation: GEN3-SOURCEOPERATORS1-20260916-G1.
Predecessor: GEN3-CERTIFIEDLOG1-20260915-G1 (H001453).

The eight-operation package is installed in the shared warm C++/GMP native worker.
It makes the retained native-pair, recurrence, creation and exchange research
reusable through current project sessions, the public CLI and all active CE domains.
The owner explicitly authorized completing the dressed-reference pass and installing
the resulting package. [Public contracts](../../../CURRENT_REVISION/engines/SLC/SOURCE_OPERATORS.md).

| Installed operation | Capability |
|---|---|
| GEN3_SOURCE_OPERATOR_WORD | Exact ordered words, commutators and tensors |
| GEN3_SOURCE_SUBSPACE | Source admission, leakage and invariant closure |
| GEN3_NATIVE_PAIR_ROOTS | Full-source quartet admission and exact root relations |
| GEN3_BLOCK_SOURCE_RECURRENCE | Metric block recurrence with retained terminal |
| GEN3_SOURCE_SPECTRAL_CREATION | Exact creation polynomial and off-shell residual |
| GEN3_SOURCE_WORD_EXCHANGE | Rank nonclosure certificates or supplied constant exchange/braid checks |
| GEN3_SOURCE_TERMINAL_ACTION | Left or dressed commutator action, kernels and rank certificates |
| GEN3_SOURCE_SPECTRAL_EXPORT | Authenticated checkpoint export without recomputation |

Every operation retains explicit source binding, basis, units, history and spectral
coordinate. Arithmetic supports Q and real quadratic fields. Dependencies retain
record digests, and all admitted source identities are checked on their full matrices.
The exact contracts distinguish incomplete recurrence, inconclusive modular tests,
certified nonclosure and coefficientwise verified constant exchange. Pair roots are
represented by exact polynomial relations; this package does not return numerical
root approximations or solve an unknown spectral-parameter-dependent R matrix.

## Research that shaped the package

The [dressed-reference result](../LI6_DRESSED_EXCHANGE1/RESULT.md) uses
Psi0=|-->+(sqrt(5)-2)|++>, with H64 eigenvalue15408-64sqrt(5). It recovers all33
complement dimensions and the11-level/3-channel recurrence. It retains the exact
commutator identity, including both terminal terms. The tested three-channel lifts
have reverse/combined ranks9/18; adding the three terminal words gives18/36.
The dressed commutator terminal action has exact rank33 and zero common kernel on
the complement. This is why terminal action and explicit left/right action belong
in the installed package.

Dressed-source creation and terminal construction:
**The test result suggests strong contact with the concept.**
Generic compact exchange closure for the three specified tested families:
**The test falsifies the concept.** General native factorized scattering remains open.
The constant-exchange positive fixture uses noncommuting Pauli operators with
nonzero products and independently checks the two-factor relation; native exact
coefficient tests, braid consistency and reversal all pass.

## Qualification, migration and consumer adoption

- Candidate:50 operation, admission, preservation and recovery checks;
  2,390 native assertions in the retained successful small fixtures.
- Dressed research:9 successful candidate domain calls,449,281 native assertions,
  and199 independent FLINT exact/modular checks.
- Installed adoption:157 checks, including exact replay of all9 dressed-source calls,
  the original64-state pair fixture, previous operator-log fixtures and the
  preserved certified spectral-log result.
- Five active CE consumers (ATOM3D, MATTER_SEARCH, STARBREAKER, MP, RH) discover and
  execute the source operations. Scoped ATOM3D aliases reject other domains.
- Public project CLI executes the ordered source word; fresh-process recovery
  exports exact creation, exchange and terminal records with zero native work and
  an unopened native worker. The installed session retains46 returned calls,
  zero failures and zero incomplete calls at adoption completion.
- All442 authenticated registry bindings pass. The activation changes47 files and
  retains every overwritten byte in `predecessor/`, sealed by `BEFORE.json`.
- All79 preserved source/model files retain their hashes. The original checkpoint
  roots, nine dressed-source records and two prior spectral records survive the
  authenticated additive migration. New immutable release files retain the prior
  release unchanged. `MIGRATION.json` and the fresh session's `CARRIED_CHECKPOINT.json`
  record explicit continuity.

The final build is warning-free under the retained undefined-behavior sanitizer.
Two build failures and the first candidate adapter's retention fault are preserved.
The repaired candidate, installation and installed adoption pass; no activation
rollback was needed. `rollback.py` retains the authenticated recovery route.
Owner training pauses and the suspended Chalkboard retain their existing authority.
Calls in other domains are installation fixtures; no curriculum or domain campaign
was launched.

`ADOPTION.json`, `INSTALLED.json`, `CANDIDATE_QUALIFICATION.json`, `RECOVERY.json`
and `SESSION_STATUS.json` hold the executable evidence. `SESSION.json` routes the
fresh ATOM3D session for subsequent work. `SOURCE_MANIFEST.json` binds the final
reports, scripts and result artifacts; installed source bytes are authenticated
by the candidate manifest and current registry.

Publication assigned H001469 after a concurrent task used H001468. The authority-only repair authenticates all corrected references and preserves the installed implementation binding, generation and calculation records. `authority_repair/REPAIR.json` connects the original activation receipt to the final authority; original metadata and the colliding draft remain preserved. Rollback accepts the authenticated corrected metadata.
