# Learning-directed source-template generator

The installed generator constructs candidate atomic source ledgers beyond the
four supplied H1/H2/H3/He3 templates. It has produced1,872 distinct retained
source histories across25 inventories. Seven inventories exceed the old N<=2Z
basis: H4,H5,H6,H7,H8,He7 andHe8. Inventory names describe the generated
proton/neutron counts; the construction assigns no physical binding or stability.

**The test result suggests strong contact with the concept.**

## Construction and provenance

Sean requested new-template generation with GEN3 learning. Codex's executable
extension rule starts from the frozen H3 andHe3 A2 source ledgers and appends
one proton or neutron at an unused neighboring N100 role and an unused adjacent
cube site. Each append executes an actual source reciprocal action pair. The
role/site search rule is an explicit new exploratory construction, recorded in
CONTRACT.json. The admitted structural projection keeps its complete frozen
seed-file hash; measured binding fields are not inputs to the generator.

Each child retains its parent's objects, cycles, terminal and custody identity,
plus its new object and connector. Independent reconstruction verifies all1,872
exact inverses. The new ledger schema and library retain these distinct histories
even when inventories and operator families coincide. New templates receive
their own injective path-to-cube placement interface. The original four-template
membership assignment is not reused. All816 placements across path sizes4–8
agree with independent exhaustive permutation enumeration.

The five connector Laplacians use the declared dimensionless source assignment
L=D-A. Installed GEN3 exact words give ranks A-1; subspace closure reaches full
dimension A. Block recurrences close and preserve terminal factors. Five exact
creation-polynomial families verify the constant reference eigenstate at0.
These source connector operators are distinct from the previous multi-cell
occupancy operators and any physical nuclear Hamiltonian.

## Learning changes execution

GEN3 fits five successive native class-balanced CART models. Its target is
whether an executed prefix can complete an eight-site source path. Candidate
features describe the occupied site mask, endpoint, free role/site neighbors,
and remaining cube connectivity. Exact native lookahead supplies labels.
Independent reconstruction verifies all643 labels. Species and phase variants
share a whole structural-family split, preventing those repeated histories
from straddling training and confirmation. Final split:370 training,135
development and138 held-out families. The final model correctly classifies
137/138 held-out families; the exception is retained in LEARNING_CONFIRMATION.

After bootstrap, each stage predicts fresh proposals before they execute.
Selection preserves inventory diversity, uses256 learned slots and64 exploration
slots, and retains all probability ties and unselected proposals. The fixed-order
comparison uses the same pool and budget; its extra outcomes are excluded from
learning and the produced-template count.

| Stage | Budget | Learned selection: can complete8 | Fixed-order selection |
|---|---:|---:|---:|
| A6 |320|210|207|
| A7 |320|265|257|
| A8 |320|320|320|

Learning changes selection in all three stages. The gain is11 continuation-capable
histories across the two nonterminal comparisons. Selection priorities remain a
learned heuristic; exact source execution decides every construction outcome.

## GEN3 capability use and recovery

CAPABILITY_MAP.json maps every installed reusable native capability. Active use
includes learning/inference/export, exact source operator words, subspace closure,
block recurrence, creation, retained operator export, exact logarithmic growth,
complete history export, and authenticated checkpoint recovery. The new C++
application adapter runs through actual ATOM3D DomainSession calls. Capabilities
requiring a different kernel, pretrained feature map, signed energy assignment,
verified pair frame or exchange lift retain those source requirements.

99 successful qualification calls and six expected rejected source-admission
controls are retained. Model, operator and history replay reconcile interrupted
application commits without refitting, recomputation or duplicate appends.
A separate fresh process restores the final model and20 exact operator records
with unchanged native counters. Global selection remains SLC-GEN3-R4 /
SLC-GEN3-CEV1-R4, domain A3D41-T18-CONTACT-R2; this installs an application adapter,
not a different global engine. The source-generation session is
ATOM3D_2bf16727919b432bb75e45ef109e390e.

## Roster use and next questions

[The native inventory bridge](../GEN3_TEMPLATE_INVENTORY_BRIDGE1/RESULT.json)
executes exact recipes for all68 formerly blocked inventories using these new
templates and the retained H3 seed. For N=qZ+r, the witness uses Z-r templates
with inventory(1,q) and r with(1,q+1).68 installed GEN3 exact matrix products
verify the resulting(Z,N), with source-template identities retained.

Instantiating and connecting the generated templates into the general assembly
roster is the next operation. These inventory recipes are not counted as68 new
assemblies. The existing roster stays3,496 targets/3,489 source assemblies.
The generator library is available independently for that next construction.

The declared search through eight nucleons is complete, with5,400 deferred
proposals retained. It is bounded, not exhaustive over all N100 assignments.
Use `./isotope-gen3 template-status`, `template-list`,
`template-export --label H4`, or `template-proposals --label H7`.
The last command applies the retained model to fresh deferred questions without
fitting. `template-run` resumes incomplete stage work; `template-recover` checks
knowledge recovery; `template-stop` records a stop request. No indefinite loop
is running. NOTICE.md records the declared endpoint and remaining questions.

The qualified campaign occupies about10.6MiB before query receipts. The300GiB
trigger preserves unique evidence, clears interrupted temporary writes and
notifies if further space decisions are needed. Work ran on the workstation's
native CPU; this short finite workload did not need helper/GPU distribution.
The stopped interaction loop, unrelated paused training, and suspended
Chalkboard remain stopped. No email was sent.

## Implementation records

GEN3_TEMPLATE_GENERATOR1 preserves an initial compiler type error and a final
operator-packaging error: the reference needed a column matrix. Its construction
and learning outcomes remain recorded. GENERATOR2 corrects that matrix shape;
GENERATOR3 consolidates learning through A8 and binds source payloads;
GENERATOR4 adds exact interrupted-commit reconciliation. Only GENERATOR4 is the
installed generator. Earlier executed Python snapshots and failed build source
are retained in their campaign folders.

Inputs, source hashes, native binary, stage predictions, learned models, full
template library, inverse receipts, interfaces, exact operator records,
VALIDATION.json and RECOVERY.json are stored beside this report.
